//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::OptionalData;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const MIGRATE_TO_NEW_INCREMENTAL_MERKLE_TREE_DISCRIMINATOR: [u8; 8] = [173, 178, 217, 56, 80, 10, 127, 242];

/// Accounts.
#[derive(Debug)]
pub struct MigrateToNewIncrementalMerkleTree {
            /// The program information account, which tracks the active tree index.

    
              
          pub protocol_config: solana_pubkey::Pubkey,
                /// The current (full) stealth pool. Used to validate it's at capacity.
/// Zero-copy account for efficient reading of large state.

    
              
          pub current_stealth_pool: solana_pubkey::Pubkey,
                /// The new stealth pool to be created.
/// Index is current + 1, ensuring sequential tree creation.
/// Zero-copy account for efficient initialization of large state.

    
              
          pub new_stealth_pool: solana_pubkey::Pubkey,
                /// The wallet specifier that authorizes this migration.

    
              
          pub admin_wallet: solana_pubkey::Pubkey,
                /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).

    
              
          pub signer: solana_pubkey::Pubkey,
                /// The fee payer who pays rent for account creation.

    
              
          pub fee_payer: solana_pubkey::Pubkey,
                /// System program for account creation.

    
              
          pub system_program: solana_pubkey::Pubkey,
      }

impl MigrateToNewIncrementalMerkleTree {
  pub fn instruction(&self, args: MigrateToNewIncrementalMerkleTreeInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: MigrateToNewIncrementalMerkleTreeInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(7+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.protocol_config,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.current_stealth_pool,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.new_stealth_pool,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.admin_wallet,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.signer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.fee_payer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = MigrateToNewIncrementalMerkleTreeInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct MigrateToNewIncrementalMerkleTreeInstructionData {
            discriminator: [u8; 8],
                  }

impl MigrateToNewIncrementalMerkleTreeInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [173, 178, 217, 56, 80, 10, 127, 242],
                                              }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for MigrateToNewIncrementalMerkleTreeInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct MigrateToNewIncrementalMerkleTreeInstructionArgs {
                  pub new_incremental_merkle_tree_index: u128,
                pub optional_data: OptionalData,
      }

impl MigrateToNewIncrementalMerkleTreeInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `MigrateToNewIncrementalMerkleTree`.
///
/// ### Accounts:
///
                ///   0. `[writable]` protocol_config
          ///   1. `[]` current_stealth_pool
                ///   2. `[writable]` new_stealth_pool
          ///   3. `[]` admin_wallet
                ///   4. `[signer]` signer
                      ///   5. `[writable, signer]` fee_payer
                ///   6. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct MigrateToNewIncrementalMerkleTreeBuilder {
            protocol_config: Option<solana_pubkey::Pubkey>,
                current_stealth_pool: Option<solana_pubkey::Pubkey>,
                new_stealth_pool: Option<solana_pubkey::Pubkey>,
                admin_wallet: Option<solana_pubkey::Pubkey>,
                signer: Option<solana_pubkey::Pubkey>,
                fee_payer: Option<solana_pubkey::Pubkey>,
                system_program: Option<solana_pubkey::Pubkey>,
                        new_incremental_merkle_tree_index: Option<u128>,
                optional_data: Option<OptionalData>,
        __program_id: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl MigrateToNewIncrementalMerkleTreeBuilder {
  pub fn new() -> Self {
    Self::default()
  }
    /// Set a custom program ID for the instruction (overrides the default).
    /// Use this when targeting a different deployment (e.g. devnet vs mainnet).
    #[inline(always)]
    pub fn program_id(&mut self, program_id: solana_pubkey::Pubkey) -> &mut Self {
        self.__program_id = Some(program_id);
        self
    }
            /// The program information account, which tracks the active tree index.
#[inline(always)]
    pub fn protocol_config(&mut self, protocol_config: solana_pubkey::Pubkey) -> &mut Self {
                        self.protocol_config = Some(protocol_config);
                    self
    }
            /// The current (full) stealth pool. Used to validate it's at capacity.
/// Zero-copy account for efficient reading of large state.
#[inline(always)]
    pub fn current_stealth_pool(&mut self, current_stealth_pool: solana_pubkey::Pubkey) -> &mut Self {
                        self.current_stealth_pool = Some(current_stealth_pool);
                    self
    }
            /// The new stealth pool to be created.
/// Index is current + 1, ensuring sequential tree creation.
/// Zero-copy account for efficient initialization of large state.
#[inline(always)]
    pub fn new_stealth_pool(&mut self, new_stealth_pool: solana_pubkey::Pubkey) -> &mut Self {
                        self.new_stealth_pool = Some(new_stealth_pool);
                    self
    }
            /// The wallet specifier that authorizes this migration.
#[inline(always)]
    pub fn admin_wallet(&mut self, admin_wallet: solana_pubkey::Pubkey) -> &mut Self {
                        self.admin_wallet = Some(admin_wallet);
                    self
    }
            /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).
#[inline(always)]
    pub fn signer(&mut self, signer: solana_pubkey::Pubkey) -> &mut Self {
                        self.signer = Some(signer);
                    self
    }
            /// The fee payer who pays rent for account creation.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: solana_pubkey::Pubkey) -> &mut Self {
                        self.fee_payer = Some(fee_payer);
                    self
    }
            /// `[optional account, default to '11111111111111111111111111111111']`
/// System program for account creation.
#[inline(always)]
    pub fn system_program(&mut self, system_program: solana_pubkey::Pubkey) -> &mut Self {
                        self.system_program = Some(system_program);
                    self
    }
                    #[inline(always)]
      pub fn new_incremental_merkle_tree_index(&mut self, new_incremental_merkle_tree_index: u128) -> &mut Self {
        self.new_incremental_merkle_tree_index = Some(new_incremental_merkle_tree_index);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = MigrateToNewIncrementalMerkleTree {
                              protocol_config: self.protocol_config.expect("protocol_config is not set"),
                                        current_stealth_pool: self.current_stealth_pool.expect("current_stealth_pool is not set"),
                                        new_stealth_pool: self.new_stealth_pool.expect("new_stealth_pool is not set"),
                                        admin_wallet: self.admin_wallet.expect("admin_wallet is not set"),
                                        signer: self.signer.expect("signer is not set"),
                                        fee_payer: self.fee_payer.expect("fee_payer is not set"),
                                        system_program: self.system_program.unwrap_or(solana_pubkey::pubkey!("11111111111111111111111111111111")),
                      };
          let args = MigrateToNewIncrementalMerkleTreeInstructionArgs {
                                                              new_incremental_merkle_tree_index: self.new_incremental_merkle_tree_index.clone().expect("new_incremental_merkle_tree_index is not set"),
                                                                  optional_data: self.optional_data.clone().expect("optional_data is not set"),
                                    };
    
    {
            let mut ix = accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts);
            if let Some(program_id) = self.__program_id {
                ix.program_id = program_id;
            }
            ix
        }
  }
}

  /// `migrate_to_new_incremental_merkle_tree` CPI accounts.
  pub struct MigrateToNewIncrementalMerkleTreeCpiAccounts<'a, 'b> {
                  /// The program information account, which tracks the active tree index.

      
                    
              pub protocol_config: &'b solana_account_info::AccountInfo<'a>,
                        /// The current (full) stealth pool. Used to validate it's at capacity.
/// Zero-copy account for efficient reading of large state.

      
                    
              pub current_stealth_pool: &'b solana_account_info::AccountInfo<'a>,
                        /// The new stealth pool to be created.
/// Index is current + 1, ensuring sequential tree creation.
/// Zero-copy account for efficient initialization of large state.

      
                    
              pub new_stealth_pool: &'b solana_account_info::AccountInfo<'a>,
                        /// The wallet specifier that authorizes this migration.

      
                    
              pub admin_wallet: &'b solana_account_info::AccountInfo<'a>,
                        /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).

      
                    
              pub signer: &'b solana_account_info::AccountInfo<'a>,
                        /// The fee payer who pays rent for account creation.

      
                    
              pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                        /// System program for account creation.

      
                    
              pub system_program: &'b solana_account_info::AccountInfo<'a>,
            }

/// `migrate_to_new_incremental_merkle_tree` CPI instruction.
pub struct MigrateToNewIncrementalMerkleTreeCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// The program information account, which tracks the active tree index.

    
              
          pub protocol_config: &'b solana_account_info::AccountInfo<'a>,
                /// The current (full) stealth pool. Used to validate it's at capacity.
/// Zero-copy account for efficient reading of large state.

    
              
          pub current_stealth_pool: &'b solana_account_info::AccountInfo<'a>,
                /// The new stealth pool to be created.
/// Index is current + 1, ensuring sequential tree creation.
/// Zero-copy account for efficient initialization of large state.

    
              
          pub new_stealth_pool: &'b solana_account_info::AccountInfo<'a>,
                /// The wallet specifier that authorizes this migration.

    
              
          pub admin_wallet: &'b solana_account_info::AccountInfo<'a>,
                /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).

    
              
          pub signer: &'b solana_account_info::AccountInfo<'a>,
                /// The fee payer who pays rent for account creation.

    
              
          pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                /// System program for account creation.

    
              
          pub system_program: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: MigrateToNewIncrementalMerkleTreeInstructionArgs,
  }

impl<'a, 'b> MigrateToNewIncrementalMerkleTreeCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: MigrateToNewIncrementalMerkleTreeCpiAccounts<'a, 'b>,
              args: MigrateToNewIncrementalMerkleTreeInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              protocol_config: accounts.protocol_config,
              current_stealth_pool: accounts.current_stealth_pool,
              new_stealth_pool: accounts.new_stealth_pool,
              admin_wallet: accounts.admin_wallet,
              signer: accounts.signer,
              fee_payer: accounts.fee_payer,
              system_program: accounts.system_program,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(7+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.protocol_config.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.current_stealth_pool.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.new_stealth_pool.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.admin_wallet.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.signer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.fee_payer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = MigrateToNewIncrementalMerkleTreeInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(8 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.protocol_config.clone());
                        account_infos.push(self.current_stealth_pool.clone());
                        account_infos.push(self.new_stealth_pool.clone());
                        account_infos.push(self.admin_wallet.clone());
                        account_infos.push(self.signer.clone());
                        account_infos.push(self.fee_payer.clone());
                        account_infos.push(self.system_program.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `MigrateToNewIncrementalMerkleTree` via CPI.
///
/// ### Accounts:
///
                ///   0. `[writable]` protocol_config
          ///   1. `[]` current_stealth_pool
                ///   2. `[writable]` new_stealth_pool
          ///   3. `[]` admin_wallet
                ///   4. `[signer]` signer
                      ///   5. `[writable, signer]` fee_payer
          ///   6. `[]` system_program
#[derive(Clone, Debug)]
pub struct MigrateToNewIncrementalMerkleTreeCpiBuilder<'a, 'b> {
  instruction: Box<MigrateToNewIncrementalMerkleTreeCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> MigrateToNewIncrementalMerkleTreeCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(MigrateToNewIncrementalMerkleTreeCpiBuilderInstruction {
      __program: program,
              protocol_config: None,
              current_stealth_pool: None,
              new_stealth_pool: None,
              admin_wallet: None,
              signer: None,
              fee_payer: None,
              system_program: None,
                                            new_incremental_merkle_tree_index: None,
                                optional_data: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// The program information account, which tracks the active tree index.
#[inline(always)]
    pub fn protocol_config(&mut self, protocol_config: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.protocol_config = Some(protocol_config);
                    self
    }
      /// The current (full) stealth pool. Used to validate it's at capacity.
/// Zero-copy account for efficient reading of large state.
#[inline(always)]
    pub fn current_stealth_pool(&mut self, current_stealth_pool: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.current_stealth_pool = Some(current_stealth_pool);
                    self
    }
      /// The new stealth pool to be created.
/// Index is current + 1, ensuring sequential tree creation.
/// Zero-copy account for efficient initialization of large state.
#[inline(always)]
    pub fn new_stealth_pool(&mut self, new_stealth_pool: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.new_stealth_pool = Some(new_stealth_pool);
                    self
    }
      /// The wallet specifier that authorizes this migration.
#[inline(always)]
    pub fn admin_wallet(&mut self, admin_wallet: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.admin_wallet = Some(admin_wallet);
                    self
    }
      /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).
#[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.signer = Some(signer);
                    self
    }
      /// The fee payer who pays rent for account creation.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.fee_payer = Some(fee_payer);
                    self
    }
      /// System program for account creation.
#[inline(always)]
    pub fn system_program(&mut self, system_program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.system_program = Some(system_program);
                    self
    }
                    #[inline(always)]
      pub fn new_incremental_merkle_tree_index(&mut self, new_incremental_merkle_tree_index: u128) -> &mut Self {
        self.instruction.new_incremental_merkle_tree_index = Some(new_incremental_merkle_tree_index);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.instruction.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = MigrateToNewIncrementalMerkleTreeInstructionArgs {
                                                              new_incremental_merkle_tree_index: self.instruction.new_incremental_merkle_tree_index.clone().expect("new_incremental_merkle_tree_index is not set"),
                                                                  optional_data: self.instruction.optional_data.clone().expect("optional_data is not set"),
                                    };
        let instruction = MigrateToNewIncrementalMerkleTreeCpi {
        __program: self.instruction.__program,
                  
          protocol_config: self.instruction.protocol_config.expect("protocol_config is not set"),
                  
          current_stealth_pool: self.instruction.current_stealth_pool.expect("current_stealth_pool is not set"),
                  
          new_stealth_pool: self.instruction.new_stealth_pool.expect("new_stealth_pool is not set"),
                  
          admin_wallet: self.instruction.admin_wallet.expect("admin_wallet is not set"),
                  
          signer: self.instruction.signer.expect("signer is not set"),
                  
          fee_payer: self.instruction.fee_payer.expect("fee_payer is not set"),
                  
          system_program: self.instruction.system_program.expect("system_program is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct MigrateToNewIncrementalMerkleTreeCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            protocol_config: Option<&'b solana_account_info::AccountInfo<'a>>,
                current_stealth_pool: Option<&'b solana_account_info::AccountInfo<'a>>,
                new_stealth_pool: Option<&'b solana_account_info::AccountInfo<'a>>,
                admin_wallet: Option<&'b solana_account_info::AccountInfo<'a>>,
                signer: Option<&'b solana_account_info::AccountInfo<'a>>,
                fee_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
                system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
                        new_incremental_merkle_tree_index: Option<u128>,
                optional_data: Option<OptionalData>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

