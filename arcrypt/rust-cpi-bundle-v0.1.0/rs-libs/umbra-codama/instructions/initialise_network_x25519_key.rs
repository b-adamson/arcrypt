//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::ArciumX25519PublicKey;
use crate::types::OptionalData;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const INITIALISE_NETWORK_X25519_KEY_DISCRIMINATOR: [u8; 8] = [92, 57, 84, 196, 77, 90, 236, 81];

/// Accounts.
#[derive(Debug)]
pub struct InitialiseNetworkX25519Key {
            /// The NETWORK X25519 key account to be initialized.
/// This is a singleton PDA for the entire program.

    
              
          pub network_key: solana_pubkey::Pubkey,
                /// The wallet specifier that authorizes this initialization.

    
              
          pub admin_wallet: solana_pubkey::Pubkey,
                /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS.

    
              
          pub signer: solana_pubkey::Pubkey,
                /// The fee payer who pays rent for account creation.

    
              
          pub fee_payer: solana_pubkey::Pubkey,
                /// System program for account creation.

    
              
          pub system_program: solana_pubkey::Pubkey,
      }

impl InitialiseNetworkX25519Key {
  pub fn instruction(&self, args: InitialiseNetworkX25519KeyInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: InitialiseNetworkX25519KeyInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(5+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.network_key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.admin_wallet,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.signer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.fee_payer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = InitialiseNetworkX25519KeyInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct InitialiseNetworkX25519KeyInstructionData {
            discriminator: [u8; 8],
                  }

impl InitialiseNetworkX25519KeyInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [92, 57, 84, 196, 77, 90, 236, 81],
                                              }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for InitialiseNetworkX25519KeyInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct InitialiseNetworkX25519KeyInstructionArgs {
                  pub network_x25519_public_key: ArciumX25519PublicKey,
                pub optional_data: OptionalData,
      }

impl InitialiseNetworkX25519KeyInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `InitialiseNetworkX25519Key`.
///
/// ### Accounts:
///
                ///   0. `[writable]` network_key
          ///   1. `[]` admin_wallet
                ///   2. `[signer]` signer
                      ///   3. `[writable, signer]` fee_payer
                ///   4. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct InitialiseNetworkX25519KeyBuilder {
            network_key: Option<solana_pubkey::Pubkey>,
                admin_wallet: Option<solana_pubkey::Pubkey>,
                signer: Option<solana_pubkey::Pubkey>,
                fee_payer: Option<solana_pubkey::Pubkey>,
                system_program: Option<solana_pubkey::Pubkey>,
                        network_x25519_public_key: Option<ArciumX25519PublicKey>,
                optional_data: Option<OptionalData>,
        __program_id: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl InitialiseNetworkX25519KeyBuilder {
  pub fn new() -> Self {
    Self::default()
  }
    /// Set a custom program ID for the instruction (overrides the default).
    /// Use this when targeting a different deployment (e.g. devnet vs mainnet).
    #[inline(always)]
    pub fn program_id(&mut self, program_id: solana_pubkey::Pubkey) -> &mut Self {
        self.__program_id = Some(program_id);
        self
    }
            /// The NETWORK X25519 key account to be initialized.
/// This is a singleton PDA for the entire program.
#[inline(always)]
    pub fn network_key(&mut self, network_key: solana_pubkey::Pubkey) -> &mut Self {
                        self.network_key = Some(network_key);
                    self
    }
            /// The wallet specifier that authorizes this initialization.
#[inline(always)]
    pub fn admin_wallet(&mut self, admin_wallet: solana_pubkey::Pubkey) -> &mut Self {
                        self.admin_wallet = Some(admin_wallet);
                    self
    }
            /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS.
#[inline(always)]
    pub fn signer(&mut self, signer: solana_pubkey::Pubkey) -> &mut Self {
                        self.signer = Some(signer);
                    self
    }
            /// The fee payer who pays rent for account creation.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: solana_pubkey::Pubkey) -> &mut Self {
                        self.fee_payer = Some(fee_payer);
                    self
    }
            /// `[optional account, default to '11111111111111111111111111111111']`
/// System program for account creation.
#[inline(always)]
    pub fn system_program(&mut self, system_program: solana_pubkey::Pubkey) -> &mut Self {
                        self.system_program = Some(system_program);
                    self
    }
                    #[inline(always)]
      pub fn network_x25519_public_key(&mut self, network_x25519_public_key: ArciumX25519PublicKey) -> &mut Self {
        self.network_x25519_public_key = Some(network_x25519_public_key);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = InitialiseNetworkX25519Key {
                              network_key: self.network_key.expect("network_key is not set"),
                                        admin_wallet: self.admin_wallet.expect("admin_wallet is not set"),
                                        signer: self.signer.expect("signer is not set"),
                                        fee_payer: self.fee_payer.expect("fee_payer is not set"),
                                        system_program: self.system_program.unwrap_or(solana_pubkey::pubkey!("11111111111111111111111111111111")),
                      };
          let args = InitialiseNetworkX25519KeyInstructionArgs {
                                                              network_x25519_public_key: self.network_x25519_public_key.clone().expect("network_x25519_public_key is not set"),
                                                                  optional_data: self.optional_data.clone().expect("optional_data is not set"),
                                    };
    
    {
            let mut ix = accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts);
            if let Some(program_id) = self.__program_id {
                ix.program_id = program_id;
            }
            ix
        }
  }
}

  /// `initialise_network_x25519_key` CPI accounts.
  pub struct InitialiseNetworkX25519KeyCpiAccounts<'a, 'b> {
                  /// The NETWORK X25519 key account to be initialized.
/// This is a singleton PDA for the entire program.

      
                    
              pub network_key: &'b solana_account_info::AccountInfo<'a>,
                        /// The wallet specifier that authorizes this initialization.

      
                    
              pub admin_wallet: &'b solana_account_info::AccountInfo<'a>,
                        /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS.

      
                    
              pub signer: &'b solana_account_info::AccountInfo<'a>,
                        /// The fee payer who pays rent for account creation.

      
                    
              pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                        /// System program for account creation.

      
                    
              pub system_program: &'b solana_account_info::AccountInfo<'a>,
            }

/// `initialise_network_x25519_key` CPI instruction.
pub struct InitialiseNetworkX25519KeyCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// The NETWORK X25519 key account to be initialized.
/// This is a singleton PDA for the entire program.

    
              
          pub network_key: &'b solana_account_info::AccountInfo<'a>,
                /// The wallet specifier that authorizes this initialization.

    
              
          pub admin_wallet: &'b solana_account_info::AccountInfo<'a>,
                /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS.

    
              
          pub signer: &'b solana_account_info::AccountInfo<'a>,
                /// The fee payer who pays rent for account creation.

    
              
          pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                /// System program for account creation.

    
              
          pub system_program: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: InitialiseNetworkX25519KeyInstructionArgs,
  }

impl<'a, 'b> InitialiseNetworkX25519KeyCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: InitialiseNetworkX25519KeyCpiAccounts<'a, 'b>,
              args: InitialiseNetworkX25519KeyInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              network_key: accounts.network_key,
              admin_wallet: accounts.admin_wallet,
              signer: accounts.signer,
              fee_payer: accounts.fee_payer,
              system_program: accounts.system_program,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(5+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.network_key.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.admin_wallet.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.signer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.fee_payer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = InitialiseNetworkX25519KeyInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.network_key.clone());
                        account_infos.push(self.admin_wallet.clone());
                        account_infos.push(self.signer.clone());
                        account_infos.push(self.fee_payer.clone());
                        account_infos.push(self.system_program.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `InitialiseNetworkX25519Key` via CPI.
///
/// ### Accounts:
///
                ///   0. `[writable]` network_key
          ///   1. `[]` admin_wallet
                ///   2. `[signer]` signer
                      ///   3. `[writable, signer]` fee_payer
          ///   4. `[]` system_program
#[derive(Clone, Debug)]
pub struct InitialiseNetworkX25519KeyCpiBuilder<'a, 'b> {
  instruction: Box<InitialiseNetworkX25519KeyCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> InitialiseNetworkX25519KeyCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(InitialiseNetworkX25519KeyCpiBuilderInstruction {
      __program: program,
              network_key: None,
              admin_wallet: None,
              signer: None,
              fee_payer: None,
              system_program: None,
                                            network_x25519_public_key: None,
                                optional_data: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// The NETWORK X25519 key account to be initialized.
/// This is a singleton PDA for the entire program.
#[inline(always)]
    pub fn network_key(&mut self, network_key: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.network_key = Some(network_key);
                    self
    }
      /// The wallet specifier that authorizes this initialization.
#[inline(always)]
    pub fn admin_wallet(&mut self, admin_wallet: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.admin_wallet = Some(admin_wallet);
                    self
    }
      /// The signer must match the wallet specifier's authorized address,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS.
#[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.signer = Some(signer);
                    self
    }
      /// The fee payer who pays rent for account creation.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.fee_payer = Some(fee_payer);
                    self
    }
      /// System program for account creation.
#[inline(always)]
    pub fn system_program(&mut self, system_program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.system_program = Some(system_program);
                    self
    }
                    #[inline(always)]
      pub fn network_x25519_public_key(&mut self, network_x25519_public_key: ArciumX25519PublicKey) -> &mut Self {
        self.instruction.network_x25519_public_key = Some(network_x25519_public_key);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.instruction.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = InitialiseNetworkX25519KeyInstructionArgs {
                                                              network_x25519_public_key: self.instruction.network_x25519_public_key.clone().expect("network_x25519_public_key is not set"),
                                                                  optional_data: self.instruction.optional_data.clone().expect("optional_data is not set"),
                                    };
        let instruction = InitialiseNetworkX25519KeyCpi {
        __program: self.instruction.__program,
                  
          network_key: self.instruction.network_key.expect("network_key is not set"),
                  
          admin_wallet: self.instruction.admin_wallet.expect("admin_wallet is not set"),
                  
          signer: self.instruction.signer.expect("signer is not set"),
                  
          fee_payer: self.instruction.fee_payer.expect("fee_payer is not set"),
                  
          system_program: self.instruction.system_program.expect("system_program is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct InitialiseNetworkX25519KeyCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            network_key: Option<&'b solana_account_info::AccountInfo<'a>>,
                admin_wallet: Option<&'b solana_account_info::AccountInfo<'a>>,
                signer: Option<&'b solana_account_info::AccountInfo<'a>>,
                fee_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
                system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
                        network_x25519_public_key: Option<ArciumX25519PublicKey>,
                optional_data: Option<OptionalData>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

