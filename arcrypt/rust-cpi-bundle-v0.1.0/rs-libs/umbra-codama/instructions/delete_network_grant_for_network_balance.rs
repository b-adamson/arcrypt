//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::ArciumX25519Nonce;
use crate::types::ArciumX25519PublicKey;
use crate::types::OptionalData;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const DELETE_NETWORK_GRANT_FOR_NETWORK_BALANCE_DISCRIMINATOR: [u8; 8] = [163, 209, 128, 146, 54, 132, 181, 243];

/// Accounts.
#[derive(Debug)]
pub struct DeleteNetworkGrantForNetworkBalance {
            /// The fee payer for the transaction.
/// This is separate from the signer to allow for sponsored transactions.

    
              
          pub fee_payer: solana_pubkey::Pubkey,
                /// AdminWallet PDA that stores the authorized admin address.
/// Seeds: [AdminWallet::SEED, DELETE_NETWORK_GRANT_FOR_NETWORK_BALANCE_SEED]

    
              
          pub admin_wallet: solana_pubkey::Pubkey,
                /// The network admin signer. Must match the address stored in admin_wallet,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).

    
              
          pub signer: solana_pubkey::Pubkey,
                /// The original rent payer from grant creation. Receives the rent refund
/// on close. Constrained to match `viewing_grant.rent_payer` so rent
/// can never be diverted by the caller (F-21).

    
              
          pub rent_payer: solana_pubkey::Pubkey,
                /// The compliance grant PDA to be deleted.
/// The `close = rent_payer` constraint returns rent to the original
/// payer recorded at create time (F-21).
/// Seeds use receiver's x25519 key from instruction arg to support dual-key model.

    
              
          pub viewing_grant: solana_pubkey::Pubkey,
      }

impl DeleteNetworkGrantForNetworkBalance {
  pub fn instruction(&self, args: DeleteNetworkGrantForNetworkBalanceInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: DeleteNetworkGrantForNetworkBalanceInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(5+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.fee_payer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.admin_wallet,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.signer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.rent_payer,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.viewing_grant,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = DeleteNetworkGrantForNetworkBalanceInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct DeleteNetworkGrantForNetworkBalanceInstructionData {
            discriminator: [u8; 8],
                        }

impl DeleteNetworkGrantForNetworkBalanceInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [163, 209, 128, 146, 54, 132, 181, 243],
                                                            }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for DeleteNetworkGrantForNetworkBalanceInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct DeleteNetworkGrantForNetworkBalanceInstructionArgs {
                  pub nonce: ArciumX25519Nonce,
                pub receiver_x25519: ArciumX25519PublicKey,
                pub optional_data: OptionalData,
      }

impl DeleteNetworkGrantForNetworkBalanceInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `DeleteNetworkGrantForNetworkBalance`.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` fee_payer
          ///   1. `[]` admin_wallet
                ///   2. `[signer]` signer
                ///   3. `[writable]` rent_payer
                ///   4. `[writable]` viewing_grant
#[derive(Clone, Debug, Default)]
pub struct DeleteNetworkGrantForNetworkBalanceBuilder {
            fee_payer: Option<solana_pubkey::Pubkey>,
                admin_wallet: Option<solana_pubkey::Pubkey>,
                signer: Option<solana_pubkey::Pubkey>,
                rent_payer: Option<solana_pubkey::Pubkey>,
                viewing_grant: Option<solana_pubkey::Pubkey>,
                        nonce: Option<ArciumX25519Nonce>,
                receiver_x25519: Option<ArciumX25519PublicKey>,
                optional_data: Option<OptionalData>,
        __program_id: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl DeleteNetworkGrantForNetworkBalanceBuilder {
  pub fn new() -> Self {
    Self::default()
  }
    /// Set a custom program ID for the instruction (overrides the default).
    /// Use this when targeting a different deployment (e.g. devnet vs mainnet).
    #[inline(always)]
    pub fn program_id(&mut self, program_id: solana_pubkey::Pubkey) -> &mut Self {
        self.__program_id = Some(program_id);
        self
    }
            /// The fee payer for the transaction.
/// This is separate from the signer to allow for sponsored transactions.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: solana_pubkey::Pubkey) -> &mut Self {
                        self.fee_payer = Some(fee_payer);
                    self
    }
            /// AdminWallet PDA that stores the authorized admin address.
/// Seeds: [AdminWallet::SEED, DELETE_NETWORK_GRANT_FOR_NETWORK_BALANCE_SEED]
#[inline(always)]
    pub fn admin_wallet(&mut self, admin_wallet: solana_pubkey::Pubkey) -> &mut Self {
                        self.admin_wallet = Some(admin_wallet);
                    self
    }
            /// The network admin signer. Must match the address stored in admin_wallet,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).
#[inline(always)]
    pub fn signer(&mut self, signer: solana_pubkey::Pubkey) -> &mut Self {
                        self.signer = Some(signer);
                    self
    }
            /// The original rent payer from grant creation. Receives the rent refund
/// on close. Constrained to match `viewing_grant.rent_payer` so rent
/// can never be diverted by the caller (F-21).
#[inline(always)]
    pub fn rent_payer(&mut self, rent_payer: solana_pubkey::Pubkey) -> &mut Self {
                        self.rent_payer = Some(rent_payer);
                    self
    }
            /// The compliance grant PDA to be deleted.
/// The `close = rent_payer` constraint returns rent to the original
/// payer recorded at create time (F-21).
/// Seeds use receiver's x25519 key from instruction arg to support dual-key model.
#[inline(always)]
    pub fn viewing_grant(&mut self, viewing_grant: solana_pubkey::Pubkey) -> &mut Self {
                        self.viewing_grant = Some(viewing_grant);
                    self
    }
                    #[inline(always)]
      pub fn nonce(&mut self, nonce: ArciumX25519Nonce) -> &mut Self {
        self.nonce = Some(nonce);
        self
      }
                #[inline(always)]
      pub fn receiver_x25519(&mut self, receiver_x25519: ArciumX25519PublicKey) -> &mut Self {
        self.receiver_x25519 = Some(receiver_x25519);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = DeleteNetworkGrantForNetworkBalance {
                              fee_payer: self.fee_payer.expect("fee_payer is not set"),
                                        admin_wallet: self.admin_wallet.expect("admin_wallet is not set"),
                                        signer: self.signer.expect("signer is not set"),
                                        rent_payer: self.rent_payer.expect("rent_payer is not set"),
                                        viewing_grant: self.viewing_grant.expect("viewing_grant is not set"),
                      };
          let args = DeleteNetworkGrantForNetworkBalanceInstructionArgs {
                                                              nonce: self.nonce.clone().expect("nonce is not set"),
                                                                  receiver_x25519: self.receiver_x25519.clone().expect("receiver_x25519 is not set"),
                                                                  optional_data: self.optional_data.clone().expect("optional_data is not set"),
                                    };
    
    {
            let mut ix = accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts);
            if let Some(program_id) = self.__program_id {
                ix.program_id = program_id;
            }
            ix
        }
  }
}

  /// `delete_network_grant_for_network_balance` CPI accounts.
  pub struct DeleteNetworkGrantForNetworkBalanceCpiAccounts<'a, 'b> {
                  /// The fee payer for the transaction.
/// This is separate from the signer to allow for sponsored transactions.

      
                    
              pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                        /// AdminWallet PDA that stores the authorized admin address.
/// Seeds: [AdminWallet::SEED, DELETE_NETWORK_GRANT_FOR_NETWORK_BALANCE_SEED]

      
                    
              pub admin_wallet: &'b solana_account_info::AccountInfo<'a>,
                        /// The network admin signer. Must match the address stored in admin_wallet,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).

      
                    
              pub signer: &'b solana_account_info::AccountInfo<'a>,
                        /// The original rent payer from grant creation. Receives the rent refund
/// on close. Constrained to match `viewing_grant.rent_payer` so rent
/// can never be diverted by the caller (F-21).

      
                    
              pub rent_payer: &'b solana_account_info::AccountInfo<'a>,
                        /// The compliance grant PDA to be deleted.
/// The `close = rent_payer` constraint returns rent to the original
/// payer recorded at create time (F-21).
/// Seeds use receiver's x25519 key from instruction arg to support dual-key model.

      
                    
              pub viewing_grant: &'b solana_account_info::AccountInfo<'a>,
            }

/// `delete_network_grant_for_network_balance` CPI instruction.
pub struct DeleteNetworkGrantForNetworkBalanceCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// The fee payer for the transaction.
/// This is separate from the signer to allow for sponsored transactions.

    
              
          pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                /// AdminWallet PDA that stores the authorized admin address.
/// Seeds: [AdminWallet::SEED, DELETE_NETWORK_GRANT_FOR_NETWORK_BALANCE_SEED]

    
              
          pub admin_wallet: &'b solana_account_info::AccountInfo<'a>,
                /// The network admin signer. Must match the address stored in admin_wallet,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).

    
              
          pub signer: &'b solana_account_info::AccountInfo<'a>,
                /// The original rent payer from grant creation. Receives the rent refund
/// on close. Constrained to match `viewing_grant.rent_payer` so rent
/// can never be diverted by the caller (F-21).

    
              
          pub rent_payer: &'b solana_account_info::AccountInfo<'a>,
                /// The compliance grant PDA to be deleted.
/// The `close = rent_payer` constraint returns rent to the original
/// payer recorded at create time (F-21).
/// Seeds use receiver's x25519 key from instruction arg to support dual-key model.

    
              
          pub viewing_grant: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: DeleteNetworkGrantForNetworkBalanceInstructionArgs,
  }

impl<'a, 'b> DeleteNetworkGrantForNetworkBalanceCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: DeleteNetworkGrantForNetworkBalanceCpiAccounts<'a, 'b>,
              args: DeleteNetworkGrantForNetworkBalanceInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              fee_payer: accounts.fee_payer,
              admin_wallet: accounts.admin_wallet,
              signer: accounts.signer,
              rent_payer: accounts.rent_payer,
              viewing_grant: accounts.viewing_grant,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(5+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.fee_payer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.admin_wallet.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.signer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.rent_payer.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.viewing_grant.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = DeleteNetworkGrantForNetworkBalanceInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.fee_payer.clone());
                        account_infos.push(self.admin_wallet.clone());
                        account_infos.push(self.signer.clone());
                        account_infos.push(self.rent_payer.clone());
                        account_infos.push(self.viewing_grant.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `DeleteNetworkGrantForNetworkBalance` via CPI.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` fee_payer
          ///   1. `[]` admin_wallet
                ///   2. `[signer]` signer
                ///   3. `[writable]` rent_payer
                ///   4. `[writable]` viewing_grant
#[derive(Clone, Debug)]
pub struct DeleteNetworkGrantForNetworkBalanceCpiBuilder<'a, 'b> {
  instruction: Box<DeleteNetworkGrantForNetworkBalanceCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> DeleteNetworkGrantForNetworkBalanceCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(DeleteNetworkGrantForNetworkBalanceCpiBuilderInstruction {
      __program: program,
              fee_payer: None,
              admin_wallet: None,
              signer: None,
              rent_payer: None,
              viewing_grant: None,
                                            nonce: None,
                                receiver_x25519: None,
                                optional_data: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// The fee payer for the transaction.
/// This is separate from the signer to allow for sponsored transactions.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.fee_payer = Some(fee_payer);
                    self
    }
      /// AdminWallet PDA that stores the authorized admin address.
/// Seeds: [AdminWallet::SEED, DELETE_NETWORK_GRANT_FOR_NETWORK_BALANCE_SEED]
#[inline(always)]
    pub fn admin_wallet(&mut self, admin_wallet: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.admin_wallet = Some(admin_wallet);
                    self
    }
      /// The network admin signer. Must match the address stored in admin_wallet,
/// OR the wallet specifier must be set to PERMISSIONLESS_ADDRESS (making it permissionless).
#[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.signer = Some(signer);
                    self
    }
      /// The original rent payer from grant creation. Receives the rent refund
/// on close. Constrained to match `viewing_grant.rent_payer` so rent
/// can never be diverted by the caller (F-21).
#[inline(always)]
    pub fn rent_payer(&mut self, rent_payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.rent_payer = Some(rent_payer);
                    self
    }
      /// The compliance grant PDA to be deleted.
/// The `close = rent_payer` constraint returns rent to the original
/// payer recorded at create time (F-21).
/// Seeds use receiver's x25519 key from instruction arg to support dual-key model.
#[inline(always)]
    pub fn viewing_grant(&mut self, viewing_grant: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.viewing_grant = Some(viewing_grant);
                    self
    }
                    #[inline(always)]
      pub fn nonce(&mut self, nonce: ArciumX25519Nonce) -> &mut Self {
        self.instruction.nonce = Some(nonce);
        self
      }
                #[inline(always)]
      pub fn receiver_x25519(&mut self, receiver_x25519: ArciumX25519PublicKey) -> &mut Self {
        self.instruction.receiver_x25519 = Some(receiver_x25519);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.instruction.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = DeleteNetworkGrantForNetworkBalanceInstructionArgs {
                                                              nonce: self.instruction.nonce.clone().expect("nonce is not set"),
                                                                  receiver_x25519: self.instruction.receiver_x25519.clone().expect("receiver_x25519 is not set"),
                                                                  optional_data: self.instruction.optional_data.clone().expect("optional_data is not set"),
                                    };
        let instruction = DeleteNetworkGrantForNetworkBalanceCpi {
        __program: self.instruction.__program,
                  
          fee_payer: self.instruction.fee_payer.expect("fee_payer is not set"),
                  
          admin_wallet: self.instruction.admin_wallet.expect("admin_wallet is not set"),
                  
          signer: self.instruction.signer.expect("signer is not set"),
                  
          rent_payer: self.instruction.rent_payer.expect("rent_payer is not set"),
                  
          viewing_grant: self.instruction.viewing_grant.expect("viewing_grant is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct DeleteNetworkGrantForNetworkBalanceCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            fee_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
                admin_wallet: Option<&'b solana_account_info::AccountInfo<'a>>,
                signer: Option<&'b solana_account_info::AccountInfo<'a>>,
                rent_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
                viewing_grant: Option<&'b solana_account_info::AccountInfo<'a>>,
                        nonce: Option<ArciumX25519Nonce>,
                receiver_x25519: Option<ArciumX25519PublicKey>,
                optional_data: Option<OptionalData>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

