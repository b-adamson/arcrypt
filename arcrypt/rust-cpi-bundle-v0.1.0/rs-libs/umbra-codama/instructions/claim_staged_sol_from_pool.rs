//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::Amount;
use crate::types::OptionalData;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const CLAIM_STAGED_SOL_FROM_POOL_DISCRIMINATOR: [u8; 8] = [129, 142, 175, 135, 163, 25, 175, 170];

/// Accounts.
#[derive(Debug)]
pub struct ClaimStagedSolFromPool {
            /// The fee payer for the transaction.
/// This is separate from the user to allow for sponsored transactions.

    
              
          pub fee_payer: solana_pubkey::Pubkey,
                /// The user who owns the encrypted token account.
/// Must sign the transaction to authorize the claim.

    
              
          pub user: solana_pubkey::Pubkey,
                /// The encrypted token account tracking the staged SOL.
/// Must be owned by the signing user (verified via PDA seeds).

    
              
          pub encrypted_token_account: solana_pubkey::Pubkey,
                /// The pool account holding the SOL.
/// SOL is transferred from here to the destination.

    
              
          pub token_pool: solana_pubkey::Pubkey,
                /// The SPL token mint associated with this encrypted token account.
/// Used for PDA derivation verification.

    
              
          pub mint: solana_pubkey::Pubkey,
                /// The destination address to receive the claimed SOL.
/// Can be any valid Solana address (wallet, PDA, etc.).
/// The user takes full responsibility for specifying a valid recipient.

    
              
          pub destination: solana_pubkey::Pubkey,
                /// The Solana System Program, required for SOL transfers.

    
              
          pub system_program: solana_pubkey::Pubkey,
      }

impl ClaimStagedSolFromPool {
  pub fn instruction(&self, args: ClaimStagedSolFromPoolInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: ClaimStagedSolFromPoolInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(7+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.fee_payer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.user,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.encrypted_token_account,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.token_pool,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.mint,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.destination,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = ClaimStagedSolFromPoolInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct ClaimStagedSolFromPoolInstructionData {
            discriminator: [u8; 8],
                  }

impl ClaimStagedSolFromPoolInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [129, 142, 175, 135, 163, 25, 175, 170],
                                              }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for ClaimStagedSolFromPoolInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct ClaimStagedSolFromPoolInstructionArgs {
                  pub amount: Amount,
                pub optional_data: OptionalData,
      }

impl ClaimStagedSolFromPoolInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `ClaimStagedSolFromPool`.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` fee_payer
                ///   1. `[signer]` user
                ///   2. `[writable]` encrypted_token_account
                ///   3. `[writable]` token_pool
          ///   4. `[]` mint
                ///   5. `[writable]` destination
                ///   6. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct ClaimStagedSolFromPoolBuilder {
            fee_payer: Option<solana_pubkey::Pubkey>,
                user: Option<solana_pubkey::Pubkey>,
                encrypted_token_account: Option<solana_pubkey::Pubkey>,
                token_pool: Option<solana_pubkey::Pubkey>,
                mint: Option<solana_pubkey::Pubkey>,
                destination: Option<solana_pubkey::Pubkey>,
                system_program: Option<solana_pubkey::Pubkey>,
                        amount: Option<Amount>,
                optional_data: Option<OptionalData>,
        __program_id: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl ClaimStagedSolFromPoolBuilder {
  pub fn new() -> Self {
    Self::default()
  }
    /// Set a custom program ID for the instruction (overrides the default).
    /// Use this when targeting a different deployment (e.g. devnet vs mainnet).
    #[inline(always)]
    pub fn program_id(&mut self, program_id: solana_pubkey::Pubkey) -> &mut Self {
        self.__program_id = Some(program_id);
        self
    }
            /// The fee payer for the transaction.
/// This is separate from the user to allow for sponsored transactions.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: solana_pubkey::Pubkey) -> &mut Self {
                        self.fee_payer = Some(fee_payer);
                    self
    }
            /// The user who owns the encrypted token account.
/// Must sign the transaction to authorize the claim.
#[inline(always)]
    pub fn user(&mut self, user: solana_pubkey::Pubkey) -> &mut Self {
                        self.user = Some(user);
                    self
    }
            /// The encrypted token account tracking the staged SOL.
/// Must be owned by the signing user (verified via PDA seeds).
#[inline(always)]
    pub fn encrypted_token_account(&mut self, encrypted_token_account: solana_pubkey::Pubkey) -> &mut Self {
                        self.encrypted_token_account = Some(encrypted_token_account);
                    self
    }
            /// The pool account holding the SOL.
/// SOL is transferred from here to the destination.
#[inline(always)]
    pub fn token_pool(&mut self, token_pool: solana_pubkey::Pubkey) -> &mut Self {
                        self.token_pool = Some(token_pool);
                    self
    }
            /// The SPL token mint associated with this encrypted token account.
/// Used for PDA derivation verification.
#[inline(always)]
    pub fn mint(&mut self, mint: solana_pubkey::Pubkey) -> &mut Self {
                        self.mint = Some(mint);
                    self
    }
            /// The destination address to receive the claimed SOL.
/// Can be any valid Solana address (wallet, PDA, etc.).
/// The user takes full responsibility for specifying a valid recipient.
#[inline(always)]
    pub fn destination(&mut self, destination: solana_pubkey::Pubkey) -> &mut Self {
                        self.destination = Some(destination);
                    self
    }
            /// `[optional account, default to '11111111111111111111111111111111']`
/// The Solana System Program, required for SOL transfers.
#[inline(always)]
    pub fn system_program(&mut self, system_program: solana_pubkey::Pubkey) -> &mut Self {
                        self.system_program = Some(system_program);
                    self
    }
                    #[inline(always)]
      pub fn amount(&mut self, amount: Amount) -> &mut Self {
        self.amount = Some(amount);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = ClaimStagedSolFromPool {
                              fee_payer: self.fee_payer.expect("fee_payer is not set"),
                                        user: self.user.expect("user is not set"),
                                        encrypted_token_account: self.encrypted_token_account.expect("encrypted_token_account is not set"),
                                        token_pool: self.token_pool.expect("token_pool is not set"),
                                        mint: self.mint.expect("mint is not set"),
                                        destination: self.destination.expect("destination is not set"),
                                        system_program: self.system_program.unwrap_or(solana_pubkey::pubkey!("11111111111111111111111111111111")),
                      };
          let args = ClaimStagedSolFromPoolInstructionArgs {
                                                              amount: self.amount.clone().expect("amount is not set"),
                                                                  optional_data: self.optional_data.clone().expect("optional_data is not set"),
                                    };
    
    {
            let mut ix = accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts);
            if let Some(program_id) = self.__program_id {
                ix.program_id = program_id;
            }
            ix
        }
  }
}

  /// `claim_staged_sol_from_pool` CPI accounts.
  pub struct ClaimStagedSolFromPoolCpiAccounts<'a, 'b> {
                  /// The fee payer for the transaction.
/// This is separate from the user to allow for sponsored transactions.

      
                    
              pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                        /// The user who owns the encrypted token account.
/// Must sign the transaction to authorize the claim.

      
                    
              pub user: &'b solana_account_info::AccountInfo<'a>,
                        /// The encrypted token account tracking the staged SOL.
/// Must be owned by the signing user (verified via PDA seeds).

      
                    
              pub encrypted_token_account: &'b solana_account_info::AccountInfo<'a>,
                        /// The pool account holding the SOL.
/// SOL is transferred from here to the destination.

      
                    
              pub token_pool: &'b solana_account_info::AccountInfo<'a>,
                        /// The SPL token mint associated with this encrypted token account.
/// Used for PDA derivation verification.

      
                    
              pub mint: &'b solana_account_info::AccountInfo<'a>,
                        /// The destination address to receive the claimed SOL.
/// Can be any valid Solana address (wallet, PDA, etc.).
/// The user takes full responsibility for specifying a valid recipient.

      
                    
              pub destination: &'b solana_account_info::AccountInfo<'a>,
                        /// The Solana System Program, required for SOL transfers.

      
                    
              pub system_program: &'b solana_account_info::AccountInfo<'a>,
            }

/// `claim_staged_sol_from_pool` CPI instruction.
pub struct ClaimStagedSolFromPoolCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// The fee payer for the transaction.
/// This is separate from the user to allow for sponsored transactions.

    
              
          pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                /// The user who owns the encrypted token account.
/// Must sign the transaction to authorize the claim.

    
              
          pub user: &'b solana_account_info::AccountInfo<'a>,
                /// The encrypted token account tracking the staged SOL.
/// Must be owned by the signing user (verified via PDA seeds).

    
              
          pub encrypted_token_account: &'b solana_account_info::AccountInfo<'a>,
                /// The pool account holding the SOL.
/// SOL is transferred from here to the destination.

    
              
          pub token_pool: &'b solana_account_info::AccountInfo<'a>,
                /// The SPL token mint associated with this encrypted token account.
/// Used for PDA derivation verification.

    
              
          pub mint: &'b solana_account_info::AccountInfo<'a>,
                /// The destination address to receive the claimed SOL.
/// Can be any valid Solana address (wallet, PDA, etc.).
/// The user takes full responsibility for specifying a valid recipient.

    
              
          pub destination: &'b solana_account_info::AccountInfo<'a>,
                /// The Solana System Program, required for SOL transfers.

    
              
          pub system_program: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: ClaimStagedSolFromPoolInstructionArgs,
  }

impl<'a, 'b> ClaimStagedSolFromPoolCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: ClaimStagedSolFromPoolCpiAccounts<'a, 'b>,
              args: ClaimStagedSolFromPoolInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              fee_payer: accounts.fee_payer,
              user: accounts.user,
              encrypted_token_account: accounts.encrypted_token_account,
              token_pool: accounts.token_pool,
              mint: accounts.mint,
              destination: accounts.destination,
              system_program: accounts.system_program,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(7+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.fee_payer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.user.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.encrypted_token_account.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.token_pool.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.mint.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.destination.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = ClaimStagedSolFromPoolInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(8 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.fee_payer.clone());
                        account_infos.push(self.user.clone());
                        account_infos.push(self.encrypted_token_account.clone());
                        account_infos.push(self.token_pool.clone());
                        account_infos.push(self.mint.clone());
                        account_infos.push(self.destination.clone());
                        account_infos.push(self.system_program.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `ClaimStagedSolFromPool` via CPI.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` fee_payer
                ///   1. `[signer]` user
                ///   2. `[writable]` encrypted_token_account
                ///   3. `[writable]` token_pool
          ///   4. `[]` mint
                ///   5. `[writable]` destination
          ///   6. `[]` system_program
#[derive(Clone, Debug)]
pub struct ClaimStagedSolFromPoolCpiBuilder<'a, 'b> {
  instruction: Box<ClaimStagedSolFromPoolCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ClaimStagedSolFromPoolCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(ClaimStagedSolFromPoolCpiBuilderInstruction {
      __program: program,
              fee_payer: None,
              user: None,
              encrypted_token_account: None,
              token_pool: None,
              mint: None,
              destination: None,
              system_program: None,
                                            amount: None,
                                optional_data: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// The fee payer for the transaction.
/// This is separate from the user to allow for sponsored transactions.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.fee_payer = Some(fee_payer);
                    self
    }
      /// The user who owns the encrypted token account.
/// Must sign the transaction to authorize the claim.
#[inline(always)]
    pub fn user(&mut self, user: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.user = Some(user);
                    self
    }
      /// The encrypted token account tracking the staged SOL.
/// Must be owned by the signing user (verified via PDA seeds).
#[inline(always)]
    pub fn encrypted_token_account(&mut self, encrypted_token_account: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.encrypted_token_account = Some(encrypted_token_account);
                    self
    }
      /// The pool account holding the SOL.
/// SOL is transferred from here to the destination.
#[inline(always)]
    pub fn token_pool(&mut self, token_pool: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.token_pool = Some(token_pool);
                    self
    }
      /// The SPL token mint associated with this encrypted token account.
/// Used for PDA derivation verification.
#[inline(always)]
    pub fn mint(&mut self, mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.mint = Some(mint);
                    self
    }
      /// The destination address to receive the claimed SOL.
/// Can be any valid Solana address (wallet, PDA, etc.).
/// The user takes full responsibility for specifying a valid recipient.
#[inline(always)]
    pub fn destination(&mut self, destination: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.destination = Some(destination);
                    self
    }
      /// The Solana System Program, required for SOL transfers.
#[inline(always)]
    pub fn system_program(&mut self, system_program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.system_program = Some(system_program);
                    self
    }
                    #[inline(always)]
      pub fn amount(&mut self, amount: Amount) -> &mut Self {
        self.instruction.amount = Some(amount);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.instruction.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = ClaimStagedSolFromPoolInstructionArgs {
                                                              amount: self.instruction.amount.clone().expect("amount is not set"),
                                                                  optional_data: self.instruction.optional_data.clone().expect("optional_data is not set"),
                                    };
        let instruction = ClaimStagedSolFromPoolCpi {
        __program: self.instruction.__program,
                  
          fee_payer: self.instruction.fee_payer.expect("fee_payer is not set"),
                  
          user: self.instruction.user.expect("user is not set"),
                  
          encrypted_token_account: self.instruction.encrypted_token_account.expect("encrypted_token_account is not set"),
                  
          token_pool: self.instruction.token_pool.expect("token_pool is not set"),
                  
          mint: self.instruction.mint.expect("mint is not set"),
                  
          destination: self.instruction.destination.expect("destination is not set"),
                  
          system_program: self.instruction.system_program.expect("system_program is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct ClaimStagedSolFromPoolCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            fee_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
                user: Option<&'b solana_account_info::AccountInfo<'a>>,
                encrypted_token_account: Option<&'b solana_account_info::AccountInfo<'a>>,
                token_pool: Option<&'b solana_account_info::AccountInfo<'a>>,
                mint: Option<&'b solana_account_info::AccountInfo<'a>>,
                destination: Option<&'b solana_account_info::AccountInfo<'a>>,
                system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
                        amount: Option<Amount>,
                optional_data: Option<OptionalData>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

