//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::AccountOffset;
use crate::types::NumberOfUnspentTransactionOutputs;
use crate::types::PoseidonHash;
use crate::types::OptionalData;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const POPULATE_PUBLIC_NULLIFIER_AND_LINKER_BUFFER_DISCRIMINATOR: [u8; 8] = [161, 150, 98, 197, 201, 78, 73, 92];

/// Accounts.
#[derive(Debug)]
pub struct PopulatePublicNullifierAndLinkerBuffer {
            /// The relayer that owns and can update this burner account.

    
              
          pub signer: solana_pubkey::Pubkey,
                /// The fee payer who pays rent for account creation and binds the
/// buffer's PDA. The same `relayer` must consume / close the buffer
/// in subsequent transactions.

    
              
          pub relayer: solana_pubkey::Pubkey,
                /// header for use during claim callback validation.

    
              
          pub user_address: solana_pubkey::Pubkey,
                /// The relayer's account containing random_generation_seed and generation_index.
/// Used for computing the unspent transaction output burner nonce.

    
              
          pub relayer_account: solana_pubkey::Pubkey,
                /// We use UncheckedAccount because:
/// 1. The account may not exist yet (first call creates it)
/// 2. Custom validation logic is required (owner, initialization status, bump)

    
              
          pub nullifier_and_linker_buffer: solana_pubkey::Pubkey,
                /// System program for creating accounts via CPI.

    
              
          pub system_program: solana_pubkey::Pubkey,
      }

impl PopulatePublicNullifierAndLinkerBuffer {
  pub fn instruction(&self, args: PopulatePublicNullifierAndLinkerBufferInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: PopulatePublicNullifierAndLinkerBufferInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(6+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.signer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.relayer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.user_address,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.relayer_account,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.nullifier_and_linker_buffer,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = PopulatePublicNullifierAndLinkerBufferInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct PopulatePublicNullifierAndLinkerBufferInstructionData {
            discriminator: [u8; 8],
                                                                              }

impl PopulatePublicNullifierAndLinkerBufferInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [161, 150, 98, 197, 201, 78, 73, 92],
                                                                                                                                                                                          }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for PopulatePublicNullifierAndLinkerBufferInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct PopulatePublicNullifierAndLinkerBufferInstructionArgs {
                  pub nullifier_and_linker_buffer_offset: AccountOffset,
                pub max_unspent_transaction_output_capacity: NumberOfUnspentTransactionOutputs,
                pub unspent_transaction_outputs_to_insert: u8,
                pub first_slot_index: u32,
                pub first_nullifier: PoseidonHash,
                pub first_linker_encryptions: [PoseidonHash; 5],
                pub first_linker_key_commitments: [PoseidonHash; 5],
                pub second_slot_index: u32,
                pub second_nullifier: PoseidonHash,
                pub second_linker_encryptions: [PoseidonHash; 5],
                pub second_linker_key_commitments: [PoseidonHash; 5],
                pub optional_data: OptionalData,
      }

impl PopulatePublicNullifierAndLinkerBufferInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `PopulatePublicNullifierAndLinkerBuffer`.
///
/// ### Accounts:
///
                ///   0. `[signer]` signer
                      ///   1. `[writable, signer]` relayer
          ///   2. `[]` user_address
                ///   3. `[writable]` relayer_account
                ///   4. `[writable]` nullifier_and_linker_buffer
                ///   5. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct PopulatePublicNullifierAndLinkerBufferBuilder {
            signer: Option<solana_pubkey::Pubkey>,
                relayer: Option<solana_pubkey::Pubkey>,
                user_address: Option<solana_pubkey::Pubkey>,
                relayer_account: Option<solana_pubkey::Pubkey>,
                nullifier_and_linker_buffer: Option<solana_pubkey::Pubkey>,
                system_program: Option<solana_pubkey::Pubkey>,
                        nullifier_and_linker_buffer_offset: Option<AccountOffset>,
                max_unspent_transaction_output_capacity: Option<NumberOfUnspentTransactionOutputs>,
                unspent_transaction_outputs_to_insert: Option<u8>,
                first_slot_index: Option<u32>,
                first_nullifier: Option<PoseidonHash>,
                first_linker_encryptions: Option<[PoseidonHash; 5]>,
                first_linker_key_commitments: Option<[PoseidonHash; 5]>,
                second_slot_index: Option<u32>,
                second_nullifier: Option<PoseidonHash>,
                second_linker_encryptions: Option<[PoseidonHash; 5]>,
                second_linker_key_commitments: Option<[PoseidonHash; 5]>,
                optional_data: Option<OptionalData>,
        __program_id: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl PopulatePublicNullifierAndLinkerBufferBuilder {
  pub fn new() -> Self {
    Self::default()
  }
    /// Set a custom program ID for the instruction (overrides the default).
    /// Use this when targeting a different deployment (e.g. devnet vs mainnet).
    #[inline(always)]
    pub fn program_id(&mut self, program_id: solana_pubkey::Pubkey) -> &mut Self {
        self.__program_id = Some(program_id);
        self
    }
            /// The relayer that owns and can update this burner account.
#[inline(always)]
    pub fn signer(&mut self, signer: solana_pubkey::Pubkey) -> &mut Self {
                        self.signer = Some(signer);
                    self
    }
            /// The fee payer who pays rent for account creation and binds the
/// buffer's PDA. The same `relayer` must consume / close the buffer
/// in subsequent transactions.
#[inline(always)]
    pub fn relayer(&mut self, relayer: solana_pubkey::Pubkey) -> &mut Self {
                        self.relayer = Some(relayer);
                    self
    }
            /// header for use during claim callback validation.
#[inline(always)]
    pub fn user_address(&mut self, user_address: solana_pubkey::Pubkey) -> &mut Self {
                        self.user_address = Some(user_address);
                    self
    }
            /// The relayer's account containing random_generation_seed and generation_index.
/// Used for computing the unspent transaction output burner nonce.
#[inline(always)]
    pub fn relayer_account(&mut self, relayer_account: solana_pubkey::Pubkey) -> &mut Self {
                        self.relayer_account = Some(relayer_account);
                    self
    }
            /// We use UncheckedAccount because:
/// 1. The account may not exist yet (first call creates it)
/// 2. Custom validation logic is required (owner, initialization status, bump)
#[inline(always)]
    pub fn nullifier_and_linker_buffer(&mut self, nullifier_and_linker_buffer: solana_pubkey::Pubkey) -> &mut Self {
                        self.nullifier_and_linker_buffer = Some(nullifier_and_linker_buffer);
                    self
    }
            /// `[optional account, default to '11111111111111111111111111111111']`
/// System program for creating accounts via CPI.
#[inline(always)]
    pub fn system_program(&mut self, system_program: solana_pubkey::Pubkey) -> &mut Self {
                        self.system_program = Some(system_program);
                    self
    }
                    #[inline(always)]
      pub fn nullifier_and_linker_buffer_offset(&mut self, nullifier_and_linker_buffer_offset: AccountOffset) -> &mut Self {
        self.nullifier_and_linker_buffer_offset = Some(nullifier_and_linker_buffer_offset);
        self
      }
                #[inline(always)]
      pub fn max_unspent_transaction_output_capacity(&mut self, max_unspent_transaction_output_capacity: NumberOfUnspentTransactionOutputs) -> &mut Self {
        self.max_unspent_transaction_output_capacity = Some(max_unspent_transaction_output_capacity);
        self
      }
                #[inline(always)]
      pub fn unspent_transaction_outputs_to_insert(&mut self, unspent_transaction_outputs_to_insert: u8) -> &mut Self {
        self.unspent_transaction_outputs_to_insert = Some(unspent_transaction_outputs_to_insert);
        self
      }
                #[inline(always)]
      pub fn first_slot_index(&mut self, first_slot_index: u32) -> &mut Self {
        self.first_slot_index = Some(first_slot_index);
        self
      }
                #[inline(always)]
      pub fn first_nullifier(&mut self, first_nullifier: PoseidonHash) -> &mut Self {
        self.first_nullifier = Some(first_nullifier);
        self
      }
                #[inline(always)]
      pub fn first_linker_encryptions(&mut self, first_linker_encryptions: [PoseidonHash; 5]) -> &mut Self {
        self.first_linker_encryptions = Some(first_linker_encryptions);
        self
      }
                #[inline(always)]
      pub fn first_linker_key_commitments(&mut self, first_linker_key_commitments: [PoseidonHash; 5]) -> &mut Self {
        self.first_linker_key_commitments = Some(first_linker_key_commitments);
        self
      }
                #[inline(always)]
      pub fn second_slot_index(&mut self, second_slot_index: u32) -> &mut Self {
        self.second_slot_index = Some(second_slot_index);
        self
      }
                #[inline(always)]
      pub fn second_nullifier(&mut self, second_nullifier: PoseidonHash) -> &mut Self {
        self.second_nullifier = Some(second_nullifier);
        self
      }
                #[inline(always)]
      pub fn second_linker_encryptions(&mut self, second_linker_encryptions: [PoseidonHash; 5]) -> &mut Self {
        self.second_linker_encryptions = Some(second_linker_encryptions);
        self
      }
                #[inline(always)]
      pub fn second_linker_key_commitments(&mut self, second_linker_key_commitments: [PoseidonHash; 5]) -> &mut Self {
        self.second_linker_key_commitments = Some(second_linker_key_commitments);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = PopulatePublicNullifierAndLinkerBuffer {
                              signer: self.signer.expect("signer is not set"),
                                        relayer: self.relayer.expect("relayer is not set"),
                                        user_address: self.user_address.expect("user_address is not set"),
                                        relayer_account: self.relayer_account.expect("relayer_account is not set"),
                                        nullifier_and_linker_buffer: self.nullifier_and_linker_buffer.expect("nullifier_and_linker_buffer is not set"),
                                        system_program: self.system_program.unwrap_or(solana_pubkey::pubkey!("11111111111111111111111111111111")),
                      };
          let args = PopulatePublicNullifierAndLinkerBufferInstructionArgs {
                                                              nullifier_and_linker_buffer_offset: self.nullifier_and_linker_buffer_offset.clone().expect("nullifier_and_linker_buffer_offset is not set"),
                                                                  max_unspent_transaction_output_capacity: self.max_unspent_transaction_output_capacity.clone().expect("max_unspent_transaction_output_capacity is not set"),
                                                                  unspent_transaction_outputs_to_insert: self.unspent_transaction_outputs_to_insert.clone().expect("unspent_transaction_outputs_to_insert is not set"),
                                                                  first_slot_index: self.first_slot_index.clone().expect("first_slot_index is not set"),
                                                                  first_nullifier: self.first_nullifier.clone().expect("first_nullifier is not set"),
                                                                  first_linker_encryptions: self.first_linker_encryptions.clone().expect("first_linker_encryptions is not set"),
                                                                  first_linker_key_commitments: self.first_linker_key_commitments.clone().expect("first_linker_key_commitments is not set"),
                                                                  second_slot_index: self.second_slot_index.clone().expect("second_slot_index is not set"),
                                                                  second_nullifier: self.second_nullifier.clone().expect("second_nullifier is not set"),
                                                                  second_linker_encryptions: self.second_linker_encryptions.clone().expect("second_linker_encryptions is not set"),
                                                                  second_linker_key_commitments: self.second_linker_key_commitments.clone().expect("second_linker_key_commitments is not set"),
                                                                  optional_data: self.optional_data.clone().expect("optional_data is not set"),
                                    };
    
    {
            let mut ix = accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts);
            if let Some(program_id) = self.__program_id {
                ix.program_id = program_id;
            }
            ix
        }
  }
}

  /// `populate_public_nullifier_and_linker_buffer` CPI accounts.
  pub struct PopulatePublicNullifierAndLinkerBufferCpiAccounts<'a, 'b> {
                  /// The relayer that owns and can update this burner account.

      
                    
              pub signer: &'b solana_account_info::AccountInfo<'a>,
                        /// The fee payer who pays rent for account creation and binds the
/// buffer's PDA. The same `relayer` must consume / close the buffer
/// in subsequent transactions.

      
                    
              pub relayer: &'b solana_account_info::AccountInfo<'a>,
                        /// header for use during claim callback validation.

      
                    
              pub user_address: &'b solana_account_info::AccountInfo<'a>,
                        /// The relayer's account containing random_generation_seed and generation_index.
/// Used for computing the unspent transaction output burner nonce.

      
                    
              pub relayer_account: &'b solana_account_info::AccountInfo<'a>,
                        /// We use UncheckedAccount because:
/// 1. The account may not exist yet (first call creates it)
/// 2. Custom validation logic is required (owner, initialization status, bump)

      
                    
              pub nullifier_and_linker_buffer: &'b solana_account_info::AccountInfo<'a>,
                        /// System program for creating accounts via CPI.

      
                    
              pub system_program: &'b solana_account_info::AccountInfo<'a>,
            }

/// `populate_public_nullifier_and_linker_buffer` CPI instruction.
pub struct PopulatePublicNullifierAndLinkerBufferCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// The relayer that owns and can update this burner account.

    
              
          pub signer: &'b solana_account_info::AccountInfo<'a>,
                /// The fee payer who pays rent for account creation and binds the
/// buffer's PDA. The same `relayer` must consume / close the buffer
/// in subsequent transactions.

    
              
          pub relayer: &'b solana_account_info::AccountInfo<'a>,
                /// header for use during claim callback validation.

    
              
          pub user_address: &'b solana_account_info::AccountInfo<'a>,
                /// The relayer's account containing random_generation_seed and generation_index.
/// Used for computing the unspent transaction output burner nonce.

    
              
          pub relayer_account: &'b solana_account_info::AccountInfo<'a>,
                /// We use UncheckedAccount because:
/// 1. The account may not exist yet (first call creates it)
/// 2. Custom validation logic is required (owner, initialization status, bump)

    
              
          pub nullifier_and_linker_buffer: &'b solana_account_info::AccountInfo<'a>,
                /// System program for creating accounts via CPI.

    
              
          pub system_program: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: PopulatePublicNullifierAndLinkerBufferInstructionArgs,
  }

impl<'a, 'b> PopulatePublicNullifierAndLinkerBufferCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: PopulatePublicNullifierAndLinkerBufferCpiAccounts<'a, 'b>,
              args: PopulatePublicNullifierAndLinkerBufferInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              signer: accounts.signer,
              relayer: accounts.relayer,
              user_address: accounts.user_address,
              relayer_account: accounts.relayer_account,
              nullifier_and_linker_buffer: accounts.nullifier_and_linker_buffer,
              system_program: accounts.system_program,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(6+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.signer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.relayer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.user_address.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.relayer_account.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.nullifier_and_linker_buffer.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = PopulatePublicNullifierAndLinkerBufferInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(7 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.signer.clone());
                        account_infos.push(self.relayer.clone());
                        account_infos.push(self.user_address.clone());
                        account_infos.push(self.relayer_account.clone());
                        account_infos.push(self.nullifier_and_linker_buffer.clone());
                        account_infos.push(self.system_program.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `PopulatePublicNullifierAndLinkerBuffer` via CPI.
///
/// ### Accounts:
///
                ///   0. `[signer]` signer
                      ///   1. `[writable, signer]` relayer
          ///   2. `[]` user_address
                ///   3. `[writable]` relayer_account
                ///   4. `[writable]` nullifier_and_linker_buffer
          ///   5. `[]` system_program
#[derive(Clone, Debug)]
pub struct PopulatePublicNullifierAndLinkerBufferCpiBuilder<'a, 'b> {
  instruction: Box<PopulatePublicNullifierAndLinkerBufferCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> PopulatePublicNullifierAndLinkerBufferCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(PopulatePublicNullifierAndLinkerBufferCpiBuilderInstruction {
      __program: program,
              signer: None,
              relayer: None,
              user_address: None,
              relayer_account: None,
              nullifier_and_linker_buffer: None,
              system_program: None,
                                            nullifier_and_linker_buffer_offset: None,
                                max_unspent_transaction_output_capacity: None,
                                unspent_transaction_outputs_to_insert: None,
                                first_slot_index: None,
                                first_nullifier: None,
                                first_linker_encryptions: None,
                                first_linker_key_commitments: None,
                                second_slot_index: None,
                                second_nullifier: None,
                                second_linker_encryptions: None,
                                second_linker_key_commitments: None,
                                optional_data: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// The relayer that owns and can update this burner account.
#[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.signer = Some(signer);
                    self
    }
      /// The fee payer who pays rent for account creation and binds the
/// buffer's PDA. The same `relayer` must consume / close the buffer
/// in subsequent transactions.
#[inline(always)]
    pub fn relayer(&mut self, relayer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.relayer = Some(relayer);
                    self
    }
      /// header for use during claim callback validation.
#[inline(always)]
    pub fn user_address(&mut self, user_address: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.user_address = Some(user_address);
                    self
    }
      /// The relayer's account containing random_generation_seed and generation_index.
/// Used for computing the unspent transaction output burner nonce.
#[inline(always)]
    pub fn relayer_account(&mut self, relayer_account: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.relayer_account = Some(relayer_account);
                    self
    }
      /// We use UncheckedAccount because:
/// 1. The account may not exist yet (first call creates it)
/// 2. Custom validation logic is required (owner, initialization status, bump)
#[inline(always)]
    pub fn nullifier_and_linker_buffer(&mut self, nullifier_and_linker_buffer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.nullifier_and_linker_buffer = Some(nullifier_and_linker_buffer);
                    self
    }
      /// System program for creating accounts via CPI.
#[inline(always)]
    pub fn system_program(&mut self, system_program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.system_program = Some(system_program);
                    self
    }
                    #[inline(always)]
      pub fn nullifier_and_linker_buffer_offset(&mut self, nullifier_and_linker_buffer_offset: AccountOffset) -> &mut Self {
        self.instruction.nullifier_and_linker_buffer_offset = Some(nullifier_and_linker_buffer_offset);
        self
      }
                #[inline(always)]
      pub fn max_unspent_transaction_output_capacity(&mut self, max_unspent_transaction_output_capacity: NumberOfUnspentTransactionOutputs) -> &mut Self {
        self.instruction.max_unspent_transaction_output_capacity = Some(max_unspent_transaction_output_capacity);
        self
      }
                #[inline(always)]
      pub fn unspent_transaction_outputs_to_insert(&mut self, unspent_transaction_outputs_to_insert: u8) -> &mut Self {
        self.instruction.unspent_transaction_outputs_to_insert = Some(unspent_transaction_outputs_to_insert);
        self
      }
                #[inline(always)]
      pub fn first_slot_index(&mut self, first_slot_index: u32) -> &mut Self {
        self.instruction.first_slot_index = Some(first_slot_index);
        self
      }
                #[inline(always)]
      pub fn first_nullifier(&mut self, first_nullifier: PoseidonHash) -> &mut Self {
        self.instruction.first_nullifier = Some(first_nullifier);
        self
      }
                #[inline(always)]
      pub fn first_linker_encryptions(&mut self, first_linker_encryptions: [PoseidonHash; 5]) -> &mut Self {
        self.instruction.first_linker_encryptions = Some(first_linker_encryptions);
        self
      }
                #[inline(always)]
      pub fn first_linker_key_commitments(&mut self, first_linker_key_commitments: [PoseidonHash; 5]) -> &mut Self {
        self.instruction.first_linker_key_commitments = Some(first_linker_key_commitments);
        self
      }
                #[inline(always)]
      pub fn second_slot_index(&mut self, second_slot_index: u32) -> &mut Self {
        self.instruction.second_slot_index = Some(second_slot_index);
        self
      }
                #[inline(always)]
      pub fn second_nullifier(&mut self, second_nullifier: PoseidonHash) -> &mut Self {
        self.instruction.second_nullifier = Some(second_nullifier);
        self
      }
                #[inline(always)]
      pub fn second_linker_encryptions(&mut self, second_linker_encryptions: [PoseidonHash; 5]) -> &mut Self {
        self.instruction.second_linker_encryptions = Some(second_linker_encryptions);
        self
      }
                #[inline(always)]
      pub fn second_linker_key_commitments(&mut self, second_linker_key_commitments: [PoseidonHash; 5]) -> &mut Self {
        self.instruction.second_linker_key_commitments = Some(second_linker_key_commitments);
        self
      }
                #[inline(always)]
      pub fn optional_data(&mut self, optional_data: OptionalData) -> &mut Self {
        self.instruction.optional_data = Some(optional_data);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = PopulatePublicNullifierAndLinkerBufferInstructionArgs {
                                                              nullifier_and_linker_buffer_offset: self.instruction.nullifier_and_linker_buffer_offset.clone().expect("nullifier_and_linker_buffer_offset is not set"),
                                                                  max_unspent_transaction_output_capacity: self.instruction.max_unspent_transaction_output_capacity.clone().expect("max_unspent_transaction_output_capacity is not set"),
                                                                  unspent_transaction_outputs_to_insert: self.instruction.unspent_transaction_outputs_to_insert.clone().expect("unspent_transaction_outputs_to_insert is not set"),
                                                                  first_slot_index: self.instruction.first_slot_index.clone().expect("first_slot_index is not set"),
                                                                  first_nullifier: self.instruction.first_nullifier.clone().expect("first_nullifier is not set"),
                                                                  first_linker_encryptions: self.instruction.first_linker_encryptions.clone().expect("first_linker_encryptions is not set"),
                                                                  first_linker_key_commitments: self.instruction.first_linker_key_commitments.clone().expect("first_linker_key_commitments is not set"),
                                                                  second_slot_index: self.instruction.second_slot_index.clone().expect("second_slot_index is not set"),
                                                                  second_nullifier: self.instruction.second_nullifier.clone().expect("second_nullifier is not set"),
                                                                  second_linker_encryptions: self.instruction.second_linker_encryptions.clone().expect("second_linker_encryptions is not set"),
                                                                  second_linker_key_commitments: self.instruction.second_linker_key_commitments.clone().expect("second_linker_key_commitments is not set"),
                                                                  optional_data: self.instruction.optional_data.clone().expect("optional_data is not set"),
                                    };
        let instruction = PopulatePublicNullifierAndLinkerBufferCpi {
        __program: self.instruction.__program,
                  
          signer: self.instruction.signer.expect("signer is not set"),
                  
          relayer: self.instruction.relayer.expect("relayer is not set"),
                  
          user_address: self.instruction.user_address.expect("user_address is not set"),
                  
          relayer_account: self.instruction.relayer_account.expect("relayer_account is not set"),
                  
          nullifier_and_linker_buffer: self.instruction.nullifier_and_linker_buffer.expect("nullifier_and_linker_buffer is not set"),
                  
          system_program: self.instruction.system_program.expect("system_program is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct PopulatePublicNullifierAndLinkerBufferCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            signer: Option<&'b solana_account_info::AccountInfo<'a>>,
                relayer: Option<&'b solana_account_info::AccountInfo<'a>>,
                user_address: Option<&'b solana_account_info::AccountInfo<'a>>,
                relayer_account: Option<&'b solana_account_info::AccountInfo<'a>>,
                nullifier_and_linker_buffer: Option<&'b solana_account_info::AccountInfo<'a>>,
                system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
                        nullifier_and_linker_buffer_offset: Option<AccountOffset>,
                max_unspent_transaction_output_capacity: Option<NumberOfUnspentTransactionOutputs>,
                unspent_transaction_outputs_to_insert: Option<u8>,
                first_slot_index: Option<u32>,
                first_nullifier: Option<PoseidonHash>,
                first_linker_encryptions: Option<[PoseidonHash; 5]>,
                first_linker_key_commitments: Option<[PoseidonHash; 5]>,
                second_slot_index: Option<u32>,
                second_nullifier: Option<PoseidonHash>,
                second_linker_encryptions: Option<[PoseidonHash; 5]>,
                second_linker_key_commitments: Option<[PoseidonHash; 5]>,
                optional_data: Option<OptionalData>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

