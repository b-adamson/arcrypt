//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::VersionByte;
use crate::types::CanonicalBump;
use crate::types::PoseidonHash;
use crate::types::UnixEpochTimestamp;
use crate::types::PoseidonCiphertext;
use crate::types::Groth16ProofA;
use crate::types::Groth16ProofB;
use crate::types::Groth16ProofC;
use crate::types::AesEncryptedUnspentTransactionOutputData;
use crate::types::OptionalData;
use crate::types::ReservedByte;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct PublicStealthPoolDepositInputBuffer {
pub discriminator: [u8; 8],
pub version_byte: VersionByte,
pub canonical_bump: CanonicalBump,
/// H2 commitment for the unspent transaction output insertion
pub insertion_h2_commitment: PoseidonHash,
/// Unix epoch timestamp for the unspent transaction output creation
pub insertion_timestamp: UnixEpochTimestamp,
/// First Poseidon ciphertext for linker encryption
pub linker_encryption0: PoseidonCiphertext,
/// Second Poseidon ciphertext for linker encryption
pub linker_encryption1: PoseidonCiphertext,
/// First keystream commitment
pub keystream_commitment0: PoseidonHash,
/// Second keystream commitment
pub keystream_commitment1: PoseidonHash,
/// Groth16 proof element A (G1 point, 64 bytes)
pub groth16_proof_a: Groth16ProofA,
/// Groth16 proof element B (G2 point, 128 bytes)
pub groth16_proof_b: Groth16ProofB,
/// Groth16 proof element C (G1 point, 64 bytes)
pub groth16_proof_c: Groth16ProofC,
/// AES encrypted unspent transaction output data for off-chain storage/indexing
pub aes_encrypted_data: AesEncryptedUnspentTransactionOutputData,
/// Optional data field for application-specific metadata
pub optional_data: OptionalData,
pub reserved_byte: [ReservedByte; 0],
}


pub const PUBLIC_STEALTH_POOL_DEPOSIT_INPUT_BUFFER_DISCRIMINATOR: [u8; 8] = [4, 237, 219, 77, 136, 78, 117, 24];

impl PublicStealthPoolDepositInputBuffer {
      pub const LEN: usize = 563;
  
  
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for PublicStealthPoolDepositInputBuffer {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_public_stealth_pool_deposit_input_buffer(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<PublicStealthPoolDepositInputBuffer>, std::io::Error> {
  let accounts = fetch_all_public_stealth_pool_deposit_input_buffer(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_public_stealth_pool_deposit_input_buffer(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<PublicStealthPoolDepositInputBuffer>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<PublicStealthPoolDepositInputBuffer>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::new(std::io::ErrorKind::Other, format!("Account not found: {}", address)))?;
      let data = PublicStealthPoolDepositInputBuffer::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_public_stealth_pool_deposit_input_buffer(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<PublicStealthPoolDepositInputBuffer>, std::io::Error> {
    let accounts = fetch_all_maybe_public_stealth_pool_deposit_input_buffer(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_public_stealth_pool_deposit_input_buffer(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<PublicStealthPoolDepositInputBuffer>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<PublicStealthPoolDepositInputBuffer>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = PublicStealthPoolDepositInputBuffer::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for PublicStealthPoolDepositInputBuffer {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for PublicStealthPoolDepositInputBuffer {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for PublicStealthPoolDepositInputBuffer {
      fn owner() -> Pubkey {
        crate::UMBRA_ID
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for PublicStealthPoolDepositInputBuffer {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for PublicStealthPoolDepositInputBuffer {
    const DISCRIMINATOR: &[u8] = &[0; 8];
  }

