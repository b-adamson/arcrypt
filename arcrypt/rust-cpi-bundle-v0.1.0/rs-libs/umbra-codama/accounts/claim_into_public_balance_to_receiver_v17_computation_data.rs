//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::VersionByte;
use crate::types::CanonicalBump;
use crate::types::SlotNumber;
use crate::types::Amount;
use crate::types::OptionalData;
use crate::types::AccountOffset;
use solana_pubkey::Pubkey;
use crate::types::ArciumX25519PublicKey;
use crate::types::ArciumX25519Nonce;
use crate::types::ReservedByte;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ClaimIntoPublicBalanceToReceiverV17ComputationData {
pub discriminator: [u8; 8],
pub version_byte: VersionByte,
pub canonical_bump: CanonicalBump,
/// Slot at which this MPC computation was queued. Set by the
/// queue handler from `Clock::get()?.slot`. Read by
/// `close_computation_data` for the cleanup TTL gate. Field
/// position (FIRST user field) is canonical across all
/// ComputationData structs so cleanup can read at a fixed offset.
pub queued_at_slot: SlotNumber,
pub relayer_sol_fees: Amount,
pub transfer_amount: Amount,
pub protocol_fees_spl: Amount,
pub relayer_fees_spl: Amount,
pub optional_data: OptionalData,
pub pool_index: AccountOffset,
/// Nonce from the unspent transaction output burner account at queue time, validated at callback
/// to ensure the burner account wasn't modified between handler and callback.
pub nullifier_and_linker_buffer_nonce: u128,
/// The offset used to derive this PDA. Stored so the callback can
/// reconstruct signer seeds for CPI (wrapped SOL unwrap).
pub mpc_callback_data_offset: AccountOffset,
/// The receiver's wallet address. Stored so the callback can emit it
/// in the event and use it as the wrapped SOL unwrap destination.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub receiver_address: Pubkey,
/// The receiver's anonymous-mode X25519 public key, snapshotted from the
/// receiver's `EncryptedUserAccount` at queue time. Used by the callback
/// only for the event payload (Arcium handles the actual encryption to
/// this key inside the MPC).
pub receiver_anonymous_x25519_public_key: ArciumX25519PublicKey,
/// Output nonce supplied to Arcium for the receiver-re-encrypted MVK
/// ciphertext. Surfaced in the callback event so the receiver can
/// decrypt the audit-trail MVK ciphertext with their anonymous-mode key.
pub mvk_output_nonce: ArciumX25519Nonce,
pub reserved_byte: [ReservedByte; 0],
}


pub const CLAIM_INTO_PUBLIC_BALANCE_TO_RECEIVER_V17_COMPUTATION_DATA_DISCRIMINATOR: [u8; 8] = [190, 44, 148, 79, 125, 77, 183, 36];

impl ClaimIntoPublicBalanceToReceiverV17ComputationData {
      pub const LEN: usize = 211;
  
  
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for ClaimIntoPublicBalanceToReceiverV17ComputationData {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_claim_into_public_balance_to_receiver_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<ClaimIntoPublicBalanceToReceiverV17ComputationData>, std::io::Error> {
  let accounts = fetch_all_claim_into_public_balance_to_receiver_v17_computation_data(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_claim_into_public_balance_to_receiver_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<ClaimIntoPublicBalanceToReceiverV17ComputationData>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<ClaimIntoPublicBalanceToReceiverV17ComputationData>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::new(std::io::ErrorKind::Other, format!("Account not found: {}", address)))?;
      let data = ClaimIntoPublicBalanceToReceiverV17ComputationData::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_claim_into_public_balance_to_receiver_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<ClaimIntoPublicBalanceToReceiverV17ComputationData>, std::io::Error> {
    let accounts = fetch_all_maybe_claim_into_public_balance_to_receiver_v17_computation_data(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_claim_into_public_balance_to_receiver_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<ClaimIntoPublicBalanceToReceiverV17ComputationData>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<ClaimIntoPublicBalanceToReceiverV17ComputationData>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = ClaimIntoPublicBalanceToReceiverV17ComputationData::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for ClaimIntoPublicBalanceToReceiverV17ComputationData {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for ClaimIntoPublicBalanceToReceiverV17ComputationData {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for ClaimIntoPublicBalanceToReceiverV17ComputationData {
      fn owner() -> Pubkey {
        crate::UMBRA_ID
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for ClaimIntoPublicBalanceToReceiverV17ComputationData {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for ClaimIntoPublicBalanceToReceiverV17ComputationData {
    const DISCRIMINATOR: &[u8] = &[0; 8];
  }

