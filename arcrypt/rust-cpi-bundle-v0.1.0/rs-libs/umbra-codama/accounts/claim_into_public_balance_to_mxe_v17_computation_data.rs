//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::VersionByte;
use crate::types::CanonicalBump;
use crate::types::SlotNumber;
use crate::types::Amount;
use crate::types::OptionalData;
use crate::types::AccountOffset;
use solana_pubkey::Pubkey;
use crate::types::ArciumX25519PublicKey;
use crate::types::ArciumX25519Nonce;
use crate::types::RescueCiphertext;
use crate::types::ReservedByte;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ClaimIntoPublicBalanceToMxeV17ComputationData {
pub discriminator: [u8; 8],
pub version_byte: VersionByte,
pub canonical_bump: CanonicalBump,
/// Slot at which this MPC computation was queued. Set by the
/// queue handler from `Clock::get()?.slot`. Read by
/// `close_computation_data` for the cleanup TTL gate. Field
/// position (FIRST user field) is canonical across all
/// ComputationData structs so cleanup can read at a fixed offset.
pub queued_at_slot: SlotNumber,
pub relayer_sol_fees: Amount,
pub transfer_amount: Amount,
pub protocol_fees_spl: Amount,
pub relayer_fees_spl: Amount,
pub optional_data: OptionalData,
pub pool_index: AccountOffset,
/// Nonce from the unspent transaction output burner account at queue time, validated at callback
/// to ensure the burner account wasn't modified between handler and callback.
pub nullifier_and_linker_buffer_nonce: u128,
/// The offset used to derive this PDA. Stored so the callback can
/// reconstruct signer seeds for CPI (wrapped SOL unwrap).
pub mpc_callback_data_offset: AccountOffset,
/// The receiver's wallet address. Stored so the callback can emit it
/// in the event and use it as the wrapped SOL unwrap destination.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub receiver_address: Pubkey,
/// Offset of the `ClaimerMasterViewingKeyAuditTree` PDA into which the
/// callback inserts a leaf on the successful-transaction path.
pub audit_tree_offset: AccountOffset,
/// Claimer's X25519 session pubkey — echoed from the claim input buffer.
pub rescue_encryption_public_key: ArciumX25519PublicKey,
/// Claimer's X25519 session nonce — echoed from the claim input buffer.
pub rescue_encryption_nonce: ArciumX25519Nonce,
/// MVK encrypted under the claimer's session pubkey + nonce. Distinct
/// from the MXE-channel re-encrypted MVK ciphertext returned by the
/// MPC computation.
pub session_encrypted_master_viewing_key: RescueCiphertext,
pub reserved_byte: [ReservedByte; 0],
}


pub const CLAIM_INTO_PUBLIC_BALANCE_TO_MXE_V17_COMPUTATION_DATA_DISCRIMINATOR: [u8; 8] = [77, 127, 48, 52, 127, 229, 113, 67];

impl ClaimIntoPublicBalanceToMxeV17ComputationData {
      pub const LEN: usize = 259;
  
  
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for ClaimIntoPublicBalanceToMxeV17ComputationData {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_claim_into_public_balance_to_mxe_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<ClaimIntoPublicBalanceToMxeV17ComputationData>, std::io::Error> {
  let accounts = fetch_all_claim_into_public_balance_to_mxe_v17_computation_data(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_claim_into_public_balance_to_mxe_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<ClaimIntoPublicBalanceToMxeV17ComputationData>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<ClaimIntoPublicBalanceToMxeV17ComputationData>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::new(std::io::ErrorKind::Other, format!("Account not found: {}", address)))?;
      let data = ClaimIntoPublicBalanceToMxeV17ComputationData::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_claim_into_public_balance_to_mxe_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<ClaimIntoPublicBalanceToMxeV17ComputationData>, std::io::Error> {
    let accounts = fetch_all_maybe_claim_into_public_balance_to_mxe_v17_computation_data(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_claim_into_public_balance_to_mxe_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<ClaimIntoPublicBalanceToMxeV17ComputationData>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<ClaimIntoPublicBalanceToMxeV17ComputationData>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = ClaimIntoPublicBalanceToMxeV17ComputationData::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for ClaimIntoPublicBalanceToMxeV17ComputationData {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for ClaimIntoPublicBalanceToMxeV17ComputationData {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for ClaimIntoPublicBalanceToMxeV17ComputationData {
      fn owner() -> Pubkey {
        crate::UMBRA_ID
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for ClaimIntoPublicBalanceToMxeV17ComputationData {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for ClaimIntoPublicBalanceToMxeV17ComputationData {
    const DISCRIMINATOR: &[u8] = &[0; 8];
  }

