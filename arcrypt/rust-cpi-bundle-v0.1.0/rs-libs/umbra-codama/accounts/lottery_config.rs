//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::VersionByte;
use crate::types::CanonicalBump;
use crate::types::StatusBits;
use crate::types::Amount;
use crate::types::ReservedByte;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LotteryConfig {
pub discriminator: [u8; 8],
pub version_byte: VersionByte,
pub canonical_bump: CanonicalBump,
pub status_bits: StatusBits,
/// `Q` in the spec. `10^mintDecimals` — raw SPL units per one human
/// token. Set at init from the SPL mint account's `decimals` and never
/// updatable thereafter — SPL mint decimals are themselves immutable, so
/// allowing this to drift would only introduce footguns.
pub token_decimal_scale: Amount,
/// `K_C` — scales the per-token creation ticket quantum. Spec default 10.
pub creation_ticket_base_scale: Amount,
/// `K_R` — scales the per-token-second claim ticket quantum. Spec default 40.
pub claim_ticket_base_scale: Amount,
/// `A_min` — minimum rewardable raw SPL amount. Below this, ticket
/// count is forced to zero by the eligibility gate.
pub minimum_rewardable_amount: Amount,
/// `A_full` — raw SPL amount at which the size multiplier saturates
/// at 1.0 (`MULTIPLIER_SCALE_FACTOR = 1_000_000`).
pub full_size_credit_threshold: Amount,
/// `T_ref` — reference duration in seconds. Spec default 86_400 (1 day).
pub claim_reference_duration: Amount,
/// `T_min` — flash-gate threshold in seconds. Claims for UTXOs with
/// time-in-pool below this earn 0 tickets.
pub minimum_rewardable_duration: Amount,
/// `K_F` — flash-gate ramp steepness (PPM).
pub flash_gate_ramp_steepness: Amount,
/// `tau` — time-loyalty half-life in seconds.
pub time_loyalty_half_life: Amount,
/// `W_max` — maximum time-loyalty bonus in PPM. Spec default
/// 2_000_000 (= 2x).
pub max_time_loyalty_bonus: Amount,
/// `C_claim` — flat bonus for completing the eligible claim path (PPM).
pub claim_completion_bonus: Amount,
#[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
pub reserved_byte: [ReservedByte; 128],
}


pub const LOTTERY_CONFIG_DISCRIMINATOR: [u8; 8] = [174, 54, 184, 175, 81, 20, 237, 24];

impl LotteryConfig {
      pub const LEN: usize = 243;
  
  
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for LotteryConfig {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_lottery_config(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<LotteryConfig>, std::io::Error> {
  let accounts = fetch_all_lottery_config(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_lottery_config(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<LotteryConfig>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<LotteryConfig>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::new(std::io::ErrorKind::Other, format!("Account not found: {}", address)))?;
      let data = LotteryConfig::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_lottery_config(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<LotteryConfig>, std::io::Error> {
    let accounts = fetch_all_maybe_lottery_config(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_lottery_config(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<LotteryConfig>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<LotteryConfig>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = LotteryConfig::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for LotteryConfig {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for LotteryConfig {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for LotteryConfig {
      fn owner() -> Pubkey {
        crate::UMBRA_ID
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for LotteryConfig {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for LotteryConfig {
    const DISCRIMINATOR: &[u8] = &[0; 8];
  }

