//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::VersionByte;
use crate::types::CanonicalBump;
use crate::types::SlotNumber;
use crate::types::ArciumX25519Nonce;
use crate::types::ArciumX25519PublicKey;
use crate::types::Amount;
use crate::types::OptionalData;
use crate::types::AccountOffset;
use crate::types::DispatchObserverCpi;
use solana_pubkey::Pubkey;
use crate::types::InstructionDiscriminator;
use crate::types::RescueCiphertext;
use crate::types::ReservedByte;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ClaimIntoExistingNetworkBalanceV17ComputationData {
pub discriminator: [u8; 8],
pub version_byte: VersionByte,
pub canonical_bump: CanonicalBump,
/// Slot at which this MPC computation was queued. Set by the
/// queue handler from `Clock::get()?.slot`. Read by
/// `close_computation_data` for the cleanup TTL gate. Field
/// position (FIRST user field) is canonical across all
/// ComputationData structs so cleanup can read at a fixed offset.
pub queued_at_slot: SlotNumber,
pub balance_output_nonce: ArciumX25519Nonce,
pub fees_output_nonce: ArciumX25519Nonce,
pub observer_output_x25519_public_key: ArciumX25519PublicKey,
pub observer_output_nonce: ArciumX25519Nonce,
pub relayer_sol_fees: Amount,
/// Nonce from the unspent transaction output burner account at queue time, validated at callback
/// to ensure the burner account wasn't modified between handler and callback.
pub nullifier_and_linker_buffer_nonce: u128,
/// Exact lamports transferred from relayer wallet to pool in the handler for nullifier set rent.
/// The callback uses this to decrement relayer_account.staging_sol_amount after reallocation.
pub nullifier_set_staging_amount: Amount,
pub optional_data: OptionalData,
/// Stealth pool index used for nullifier set PDA derivation.
/// Captured from caller-supplied stealth_pool_index in the handler so the callback
/// burns nullifiers in the correct namespace even after a tree migration.
pub stealth_pool_index: AccountOffset,
/// When DISABLED the callback skips the observer CPI (and the queue
/// handler encrypts the observer ciphertext to the MXE pubkey for
/// confidentiality). When ENABLED the destination_program /
/// cpi_account_1 / cpi_account_2 / destination_discriminator /
/// initiator fields below are honoured.
pub dispatch_observer_cpi: DispatchObserverCpi,
/// Destination program the callback CPIs into when observer is enabled.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub destination_program: Pubkey,
/// Anchor-style 8-byte discriminator of the destination's receive instruction.
pub destination_discriminator: InstructionDiscriminator,
/// First CPI account forwarded to the destination program.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub cpi_account1: Pubkey,
/// Second CPI account forwarded to the destination program.
/// Independent initiator identity persisted at queue time; forwarded to
/// the destination program so it can verify the request it initiated.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub initiator: Pubkey,
/// Offset of the `ClaimerMasterViewingKeyAuditTree` PDA into which the
/// callback inserts a leaf on the successful-transaction path. Captured
/// here at queue time so the callback can re-derive the audit-tree PDA
/// without trusting the relayer to re-supply it.
pub audit_tree_offset: AccountOffset,
/// X25519 session pubkey under which the claimer encrypted their MVK.
/// Echoed from `claim_input_buffer.rescue_encryption_public_key`.
pub rescue_encryption_public_key: ArciumX25519PublicKey,
/// X25519 nonce paired with the session pubkey for the MVK ciphertext.
/// Echoed from `claim_input_buffer.encryption_nonce`.
pub rescue_encryption_nonce: ArciumX25519Nonce,
/// MVK ciphertext encrypted under the session pubkey + nonce above.
/// Echoed from `claim_input_buffer.encrypted_master_viewing_key`.
pub encrypted_master_viewing_key: RescueCiphertext,
/// Receiver's SOL wallet address — folded into the audit-tree leaf
/// preimage. The callback can't recover it from `receiver_token_account`
/// because the EncryptedTokenAccount PDA seeds use it but it isn't
/// stored inside that account.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub receiver_address: Pubkey,
pub reserved_byte: [ReservedByte; 0],
}


pub const CLAIM_INTO_EXISTING_NETWORK_BALANCE_V17_COMPUTATION_DATA_DISCRIMINATOR: [u8; 8] = [173, 147, 61, 177, 173, 89, 114, 139];

impl ClaimIntoExistingNetworkBalanceV17ComputationData {
      pub const LEN: usize = 412;
  
  
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for ClaimIntoExistingNetworkBalanceV17ComputationData {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_claim_into_existing_network_balance_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<ClaimIntoExistingNetworkBalanceV17ComputationData>, std::io::Error> {
  let accounts = fetch_all_claim_into_existing_network_balance_v17_computation_data(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_claim_into_existing_network_balance_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<ClaimIntoExistingNetworkBalanceV17ComputationData>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<ClaimIntoExistingNetworkBalanceV17ComputationData>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::new(std::io::ErrorKind::Other, format!("Account not found: {}", address)))?;
      let data = ClaimIntoExistingNetworkBalanceV17ComputationData::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_claim_into_existing_network_balance_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<ClaimIntoExistingNetworkBalanceV17ComputationData>, std::io::Error> {
    let accounts = fetch_all_maybe_claim_into_existing_network_balance_v17_computation_data(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_claim_into_existing_network_balance_v17_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<ClaimIntoExistingNetworkBalanceV17ComputationData>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<ClaimIntoExistingNetworkBalanceV17ComputationData>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = ClaimIntoExistingNetworkBalanceV17ComputationData::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for ClaimIntoExistingNetworkBalanceV17ComputationData {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for ClaimIntoExistingNetworkBalanceV17ComputationData {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for ClaimIntoExistingNetworkBalanceV17ComputationData {
      fn owner() -> Pubkey {
        crate::UMBRA_ID
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for ClaimIntoExistingNetworkBalanceV17ComputationData {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for ClaimIntoExistingNetworkBalanceV17ComputationData {
    const DISCRIMINATOR: &[u8] = &[0; 8];
  }

