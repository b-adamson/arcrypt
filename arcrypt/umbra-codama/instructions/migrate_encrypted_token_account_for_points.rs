//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const MIGRATE_ENCRYPTED_TOKEN_ACCOUNT_FOR_POINTS_DISCRIMINATOR: [u8; 8] = [151, 207, 126, 45, 179, 110, 11, 229];

/// Accounts.
#[derive(Debug)]
pub struct MigrateEncryptedTokenAccountForPoints {
            /// The user (token-account owner) whose `EncryptedTokenAccount` is being
/// migrated. Used only as a seed component to bind
/// `encrypted_token_account` to its canonical PDA.

    
              
          pub user_address: solana_pubkey::Pubkey,
                /// SPL token mint of the account being migrated. Pinned via the
/// `encrypted_token_account` PDA seeds below.

    
              
          pub mint: solana_pubkey::Pubkey,
                /// The encrypted token account being migrated.
/// 
/// `AccountInfo` rather than `Account<EncryptedTokenAccount>` because
/// Anchor's typed loader would fail to deserialize a pre-migration
/// account whose on-disk size is smaller than the new
/// `EncryptedTokenAccount::SIZE`. The handler validates the
/// discriminator and the pre-migration size manually. The PDA seed
/// constraint here binds the account to `(user_address, mint)` so an
/// attacker cannot supply an arbitrary AccountInfo.

    
              
          pub encrypted_token_account: solana_pubkey::Pubkey,
                /// The fee payer responsible for the rent delta required to grow
/// the account by 64 bytes.

    
              
          pub fee_payer: solana_pubkey::Pubkey,
                /// Solana System Program — used by `realloc` for rent collection.

    
              
          pub system_program: solana_pubkey::Pubkey,
      }

impl MigrateEncryptedTokenAccountForPoints {
  pub fn instruction(&self) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(&[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(5+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.user_address,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.mint,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.encrypted_token_account,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.fee_payer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let data = MigrateEncryptedTokenAccountForPointsInstructionData::new().try_to_vec().unwrap();
    
    solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct MigrateEncryptedTokenAccountForPointsInstructionData {
            discriminator: [u8; 8],
      }

impl MigrateEncryptedTokenAccountForPointsInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [151, 207, 126, 45, 179, 110, 11, 229],
                  }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for MigrateEncryptedTokenAccountForPointsInstructionData {
  fn default() -> Self {
    Self::new()
  }
}



/// Instruction builder for `MigrateEncryptedTokenAccountForPoints`.
///
/// ### Accounts:
///
          ///   0. `[]` user_address
          ///   1. `[]` mint
                ///   2. `[writable]` encrypted_token_account
                      ///   3. `[writable, signer]` fee_payer
                ///   4. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct MigrateEncryptedTokenAccountForPointsBuilder {
            user_address: Option<solana_pubkey::Pubkey>,
                mint: Option<solana_pubkey::Pubkey>,
                encrypted_token_account: Option<solana_pubkey::Pubkey>,
                fee_payer: Option<solana_pubkey::Pubkey>,
                system_program: Option<solana_pubkey::Pubkey>,
                __program_id: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl MigrateEncryptedTokenAccountForPointsBuilder {
  pub fn new() -> Self {
    Self::default()
  }
    /// Set a custom program ID for the instruction (overrides the default).
    /// Use this when targeting a different deployment (e.g. devnet vs mainnet).
    #[inline(always)]
    pub fn program_id(&mut self, program_id: solana_pubkey::Pubkey) -> &mut Self {
        self.__program_id = Some(program_id);
        self
    }
            /// The user (token-account owner) whose `EncryptedTokenAccount` is being
/// migrated. Used only as a seed component to bind
/// `encrypted_token_account` to its canonical PDA.
#[inline(always)]
    pub fn user_address(&mut self, user_address: solana_pubkey::Pubkey) -> &mut Self {
                        self.user_address = Some(user_address);
                    self
    }
            /// SPL token mint of the account being migrated. Pinned via the
/// `encrypted_token_account` PDA seeds below.
#[inline(always)]
    pub fn mint(&mut self, mint: solana_pubkey::Pubkey) -> &mut Self {
                        self.mint = Some(mint);
                    self
    }
            /// The encrypted token account being migrated.
/// 
/// `AccountInfo` rather than `Account<EncryptedTokenAccount>` because
/// Anchor's typed loader would fail to deserialize a pre-migration
/// account whose on-disk size is smaller than the new
/// `EncryptedTokenAccount::SIZE`. The handler validates the
/// discriminator and the pre-migration size manually. The PDA seed
/// constraint here binds the account to `(user_address, mint)` so an
/// attacker cannot supply an arbitrary AccountInfo.
#[inline(always)]
    pub fn encrypted_token_account(&mut self, encrypted_token_account: solana_pubkey::Pubkey) -> &mut Self {
                        self.encrypted_token_account = Some(encrypted_token_account);
                    self
    }
            /// The fee payer responsible for the rent delta required to grow
/// the account by 64 bytes.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: solana_pubkey::Pubkey) -> &mut Self {
                        self.fee_payer = Some(fee_payer);
                    self
    }
            /// `[optional account, default to '11111111111111111111111111111111']`
/// Solana System Program — used by `realloc` for rent collection.
#[inline(always)]
    pub fn system_program(&mut self, system_program: solana_pubkey::Pubkey) -> &mut Self {
                        self.system_program = Some(system_program);
                    self
    }
            /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = MigrateEncryptedTokenAccountForPoints {
                              user_address: self.user_address.expect("user_address is not set"),
                                        mint: self.mint.expect("mint is not set"),
                                        encrypted_token_account: self.encrypted_token_account.expect("encrypted_token_account is not set"),
                                        fee_payer: self.fee_payer.expect("fee_payer is not set"),
                                        system_program: self.system_program.unwrap_or(solana_pubkey::pubkey!("11111111111111111111111111111111")),
                      };
    
    accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
  }
}

  /// `migrate_encrypted_token_account_for_points` CPI accounts.
  pub struct MigrateEncryptedTokenAccountForPointsCpiAccounts<'a, 'b> {
                  /// The user (token-account owner) whose `EncryptedTokenAccount` is being
/// migrated. Used only as a seed component to bind
/// `encrypted_token_account` to its canonical PDA.

      
                    
              pub user_address: &'b solana_account_info::AccountInfo<'a>,
                        /// SPL token mint of the account being migrated. Pinned via the
/// `encrypted_token_account` PDA seeds below.

      
                    
              pub mint: &'b solana_account_info::AccountInfo<'a>,
                        /// The encrypted token account being migrated.
/// 
/// `AccountInfo` rather than `Account<EncryptedTokenAccount>` because
/// Anchor's typed loader would fail to deserialize a pre-migration
/// account whose on-disk size is smaller than the new
/// `EncryptedTokenAccount::SIZE`. The handler validates the
/// discriminator and the pre-migration size manually. The PDA seed
/// constraint here binds the account to `(user_address, mint)` so an
/// attacker cannot supply an arbitrary AccountInfo.

      
                    
              pub encrypted_token_account: &'b solana_account_info::AccountInfo<'a>,
                        /// The fee payer responsible for the rent delta required to grow
/// the account by 64 bytes.

      
                    
              pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                        /// Solana System Program — used by `realloc` for rent collection.

      
                    
              pub system_program: &'b solana_account_info::AccountInfo<'a>,
            }

/// `migrate_encrypted_token_account_for_points` CPI instruction.
pub struct MigrateEncryptedTokenAccountForPointsCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// The user (token-account owner) whose `EncryptedTokenAccount` is being
/// migrated. Used only as a seed component to bind
/// `encrypted_token_account` to its canonical PDA.

    
              
          pub user_address: &'b solana_account_info::AccountInfo<'a>,
                /// SPL token mint of the account being migrated. Pinned via the
/// `encrypted_token_account` PDA seeds below.

    
              
          pub mint: &'b solana_account_info::AccountInfo<'a>,
                /// The encrypted token account being migrated.
/// 
/// `AccountInfo` rather than `Account<EncryptedTokenAccount>` because
/// Anchor's typed loader would fail to deserialize a pre-migration
/// account whose on-disk size is smaller than the new
/// `EncryptedTokenAccount::SIZE`. The handler validates the
/// discriminator and the pre-migration size manually. The PDA seed
/// constraint here binds the account to `(user_address, mint)` so an
/// attacker cannot supply an arbitrary AccountInfo.

    
              
          pub encrypted_token_account: &'b solana_account_info::AccountInfo<'a>,
                /// The fee payer responsible for the rent delta required to grow
/// the account by 64 bytes.

    
              
          pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                /// Solana System Program — used by `realloc` for rent collection.

    
              
          pub system_program: &'b solana_account_info::AccountInfo<'a>,
        }

impl<'a, 'b> MigrateEncryptedTokenAccountForPointsCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: MigrateEncryptedTokenAccountForPointsCpiAccounts<'a, 'b>,
          ) -> Self {
    Self {
      __program: program,
              user_address: accounts.user_address,
              mint: accounts.mint,
              encrypted_token_account: accounts.encrypted_token_account,
              fee_payer: accounts.fee_payer,
              system_program: accounts.system_program,
                }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(5+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.user_address.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.mint.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.encrypted_token_account.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.fee_payer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let data = MigrateEncryptedTokenAccountForPointsInstructionData::new().try_to_vec().unwrap();
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.user_address.clone());
                        account_infos.push(self.mint.clone());
                        account_infos.push(self.encrypted_token_account.clone());
                        account_infos.push(self.fee_payer.clone());
                        account_infos.push(self.system_program.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `MigrateEncryptedTokenAccountForPoints` via CPI.
///
/// ### Accounts:
///
          ///   0. `[]` user_address
          ///   1. `[]` mint
                ///   2. `[writable]` encrypted_token_account
                      ///   3. `[writable, signer]` fee_payer
          ///   4. `[]` system_program
#[derive(Clone, Debug)]
pub struct MigrateEncryptedTokenAccountForPointsCpiBuilder<'a, 'b> {
  instruction: Box<MigrateEncryptedTokenAccountForPointsCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> MigrateEncryptedTokenAccountForPointsCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(MigrateEncryptedTokenAccountForPointsCpiBuilderInstruction {
      __program: program,
              user_address: None,
              mint: None,
              encrypted_token_account: None,
              fee_payer: None,
              system_program: None,
                                __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// The user (token-account owner) whose `EncryptedTokenAccount` is being
/// migrated. Used only as a seed component to bind
/// `encrypted_token_account` to its canonical PDA.
#[inline(always)]
    pub fn user_address(&mut self, user_address: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.user_address = Some(user_address);
                    self
    }
      /// SPL token mint of the account being migrated. Pinned via the
/// `encrypted_token_account` PDA seeds below.
#[inline(always)]
    pub fn mint(&mut self, mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.mint = Some(mint);
                    self
    }
      /// The encrypted token account being migrated.
/// 
/// `AccountInfo` rather than `Account<EncryptedTokenAccount>` because
/// Anchor's typed loader would fail to deserialize a pre-migration
/// account whose on-disk size is smaller than the new
/// `EncryptedTokenAccount::SIZE`. The handler validates the
/// discriminator and the pre-migration size manually. The PDA seed
/// constraint here binds the account to `(user_address, mint)` so an
/// attacker cannot supply an arbitrary AccountInfo.
#[inline(always)]
    pub fn encrypted_token_account(&mut self, encrypted_token_account: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.encrypted_token_account = Some(encrypted_token_account);
                    self
    }
      /// The fee payer responsible for the rent delta required to grow
/// the account by 64 bytes.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.fee_payer = Some(fee_payer);
                    self
    }
      /// Solana System Program — used by `realloc` for rent collection.
#[inline(always)]
    pub fn system_program(&mut self, system_program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.system_program = Some(system_program);
                    self
    }
            /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = MigrateEncryptedTokenAccountForPointsCpi {
        __program: self.instruction.__program,
                  
          user_address: self.instruction.user_address.expect("user_address is not set"),
                  
          mint: self.instruction.mint.expect("mint is not set"),
                  
          encrypted_token_account: self.instruction.encrypted_token_account.expect("encrypted_token_account is not set"),
                  
          fee_payer: self.instruction.fee_payer.expect("fee_payer is not set"),
                  
          system_program: self.instruction.system_program.expect("system_program is not set"),
                    };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct MigrateEncryptedTokenAccountForPointsCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            user_address: Option<&'b solana_account_info::AccountInfo<'a>>,
                mint: Option<&'b solana_account_info::AccountInfo<'a>>,
                encrypted_token_account: Option<&'b solana_account_info::AccountInfo<'a>>,
                fee_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
                system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
                /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

