//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::AccountOffset;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const CLOSE_STEALTH_POOL_DEPOSIT_INPUT_BUFFER_DISCRIMINATOR: [u8; 8] = [228, 142, 72, 195, 90, 209, 222, 156];

/// Accounts.
#[derive(Debug)]
pub struct CloseStealthPoolDepositInputBuffer {
            /// The original `fee_payer` who created the account and receives the rent
/// refund. PDA seed match enforces that this is the same `fee_payer` used
/// at create time.

    
              
          pub fee_payer: solana_pubkey::Pubkey,
                /// The confidential unspent transaction output input buffer to be closed.
/// The `close` constraint ensures rent is returned to `fee_payer`.

    
              
          pub stealth_pool_deposit_input_buffer: solana_pubkey::Pubkey,
      }

impl CloseStealthPoolDepositInputBuffer {
  pub fn instruction(&self, args: CloseStealthPoolDepositInputBufferInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: CloseStealthPoolDepositInputBufferInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(2+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.fee_payer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.stealth_pool_deposit_input_buffer,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = CloseStealthPoolDepositInputBufferInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct CloseStealthPoolDepositInputBufferInstructionData {
            discriminator: [u8; 8],
            }

impl CloseStealthPoolDepositInputBufferInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [228, 142, 72, 195, 90, 209, 222, 156],
                                }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for CloseStealthPoolDepositInputBufferInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct CloseStealthPoolDepositInputBufferInstructionArgs {
                  pub offset: AccountOffset,
      }

impl CloseStealthPoolDepositInputBufferInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `CloseStealthPoolDepositInputBuffer`.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` fee_payer
                ///   1. `[writable]` stealth_pool_deposit_input_buffer
#[derive(Clone, Debug, Default)]
pub struct CloseStealthPoolDepositInputBufferBuilder {
            fee_payer: Option<solana_pubkey::Pubkey>,
                stealth_pool_deposit_input_buffer: Option<solana_pubkey::Pubkey>,
                        offset: Option<AccountOffset>,
        __program_id: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CloseStealthPoolDepositInputBufferBuilder {
  pub fn new() -> Self {
    Self::default()
  }
    /// Set a custom program ID for the instruction (overrides the default).
    /// Use this when targeting a different deployment (e.g. devnet vs mainnet).
    #[inline(always)]
    pub fn program_id(&mut self, program_id: solana_pubkey::Pubkey) -> &mut Self {
        self.__program_id = Some(program_id);
        self
    }
            /// The original `fee_payer` who created the account and receives the rent
/// refund. PDA seed match enforces that this is the same `fee_payer` used
/// at create time.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: solana_pubkey::Pubkey) -> &mut Self {
                        self.fee_payer = Some(fee_payer);
                    self
    }
            /// The confidential unspent transaction output input buffer to be closed.
/// The `close` constraint ensures rent is returned to `fee_payer`.
#[inline(always)]
    pub fn stealth_pool_deposit_input_buffer(&mut self, stealth_pool_deposit_input_buffer: solana_pubkey::Pubkey) -> &mut Self {
                        self.stealth_pool_deposit_input_buffer = Some(stealth_pool_deposit_input_buffer);
                    self
    }
                    #[inline(always)]
      pub fn offset(&mut self, offset: AccountOffset) -> &mut Self {
        self.offset = Some(offset);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = CloseStealthPoolDepositInputBuffer {
                              fee_payer: self.fee_payer.expect("fee_payer is not set"),
                                        stealth_pool_deposit_input_buffer: self.stealth_pool_deposit_input_buffer.expect("stealth_pool_deposit_input_buffer is not set"),
                      };
          let args = CloseStealthPoolDepositInputBufferInstructionArgs {
                                                              offset: self.offset.clone().expect("offset is not set"),
                                    };
    
    {
            let mut ix = accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts);
            if let Some(program_id) = self.__program_id {
                ix.program_id = program_id;
            }
            ix
        }
  }
}

  /// `close_stealth_pool_deposit_input_buffer` CPI accounts.
  pub struct CloseStealthPoolDepositInputBufferCpiAccounts<'a, 'b> {
                  /// The original `fee_payer` who created the account and receives the rent
/// refund. PDA seed match enforces that this is the same `fee_payer` used
/// at create time.

      
                    
              pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                        /// The confidential unspent transaction output input buffer to be closed.
/// The `close` constraint ensures rent is returned to `fee_payer`.

      
                    
              pub stealth_pool_deposit_input_buffer: &'b solana_account_info::AccountInfo<'a>,
            }

/// `close_stealth_pool_deposit_input_buffer` CPI instruction.
pub struct CloseStealthPoolDepositInputBufferCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// The original `fee_payer` who created the account and receives the rent
/// refund. PDA seed match enforces that this is the same `fee_payer` used
/// at create time.

    
              
          pub fee_payer: &'b solana_account_info::AccountInfo<'a>,
                /// The confidential unspent transaction output input buffer to be closed.
/// The `close` constraint ensures rent is returned to `fee_payer`.

    
              
          pub stealth_pool_deposit_input_buffer: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: CloseStealthPoolDepositInputBufferInstructionArgs,
  }

impl<'a, 'b> CloseStealthPoolDepositInputBufferCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: CloseStealthPoolDepositInputBufferCpiAccounts<'a, 'b>,
              args: CloseStealthPoolDepositInputBufferInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              fee_payer: accounts.fee_payer,
              stealth_pool_deposit_input_buffer: accounts.stealth_pool_deposit_input_buffer,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(2+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.fee_payer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.stealth_pool_deposit_input_buffer.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = CloseStealthPoolDepositInputBufferInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::UMBRA_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.fee_payer.clone());
                        account_infos.push(self.stealth_pool_deposit_input_buffer.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `CloseStealthPoolDepositInputBuffer` via CPI.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` fee_payer
                ///   1. `[writable]` stealth_pool_deposit_input_buffer
#[derive(Clone, Debug)]
pub struct CloseStealthPoolDepositInputBufferCpiBuilder<'a, 'b> {
  instruction: Box<CloseStealthPoolDepositInputBufferCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CloseStealthPoolDepositInputBufferCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(CloseStealthPoolDepositInputBufferCpiBuilderInstruction {
      __program: program,
              fee_payer: None,
              stealth_pool_deposit_input_buffer: None,
                                            offset: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// The original `fee_payer` who created the account and receives the rent
/// refund. PDA seed match enforces that this is the same `fee_payer` used
/// at create time.
#[inline(always)]
    pub fn fee_payer(&mut self, fee_payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.fee_payer = Some(fee_payer);
                    self
    }
      /// The confidential unspent transaction output input buffer to be closed.
/// The `close` constraint ensures rent is returned to `fee_payer`.
#[inline(always)]
    pub fn stealth_pool_deposit_input_buffer(&mut self, stealth_pool_deposit_input_buffer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.stealth_pool_deposit_input_buffer = Some(stealth_pool_deposit_input_buffer);
                    self
    }
                    #[inline(always)]
      pub fn offset(&mut self, offset: AccountOffset) -> &mut Self {
        self.instruction.offset = Some(offset);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = CloseStealthPoolDepositInputBufferInstructionArgs {
                                                              offset: self.instruction.offset.clone().expect("offset is not set"),
                                    };
        let instruction = CloseStealthPoolDepositInputBufferCpi {
        __program: self.instruction.__program,
                  
          fee_payer: self.instruction.fee_payer.expect("fee_payer is not set"),
                  
          stealth_pool_deposit_input_buffer: self.instruction.stealth_pool_deposit_input_buffer.expect("stealth_pool_deposit_input_buffer is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct CloseStealthPoolDepositInputBufferCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            fee_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
                stealth_pool_deposit_input_buffer: Option<&'b solana_account_info::AccountInfo<'a>>,
                        offset: Option<AccountOffset>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

