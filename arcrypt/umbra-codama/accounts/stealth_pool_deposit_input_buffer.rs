//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::VersionByte;
use crate::types::CanonicalBump;
use crate::types::ArciumX25519PublicKey;
use crate::types::ArciumX25519Nonce;
use crate::types::RescueCiphertext;
use crate::types::FieldElement25519;
use crate::types::PoseidonHash;
use crate::types::UnixEpochTimestamp;
use crate::types::PoseidonCiphertext;
use crate::types::Groth16ProofA;
use crate::types::Groth16ProofB;
use crate::types::Groth16ProofC;
use crate::types::AesEncryptedUnspentTransactionOutputData;
use crate::types::OptionalData;
use crate::types::ReservedByte;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct StealthPoolDepositInputBuffer {
pub discriminator: [u8; 8],
pub version_byte: VersionByte,
pub canonical_bump: CanonicalBump,
/// X25519 public key the prover used as the Rescue cipher channel key
/// (ECDH against the MXE x25519 pubkey). Bound into the Groth16 public-
/// input aggregator at slots [16-17] and the Fiat-Shamir transcript by
/// the on-chain handler. The MPC reads this same key to derive its
/// shared secret for decrypting the new transaction parts (slot 5 of
/// the MPC inputs). Decoupled from the encrypted-balance key on the
/// EncryptedTokenAccount, which still governs the existing-balance and
/// output-balance ciphertexts.
pub rescue_encryption_public_key: ArciumX25519PublicKey,
/// X25519 public key the AES UTXO-recovery payload was encrypted to
/// (typically the user's master-viewing-key x25519). Recorded in the
/// callback event so indexers can identify which key holders can
/// decrypt the AES blob. Has no role in proof verification or balance
/// decryption — purely a metadata channel for off-chain recovery.
pub aes_encryption_public_key: ArciumX25519PublicKey,
/// Nonce used for the Rescue encryption scheme
pub rescue_encryption_nonce: ArciumX25519Nonce,
/// Encrypted total amount being deposited
pub rescue_encrypted_total_amount: RescueCiphertext,
/// Encrypted protocol fees
pub rescue_encrypted_protocol_fees: RescueCiphertext,
/// Encrypted lottery ticket delta (`creationTicketCount` in the create
/// circuit's polynomial — slot 2, between `protocolFee` and
/// `randomFactor`). Single 256-bit Curve25519 field element,
/// rescue-encrypted under the depositor's session key. Matches the
/// Arcis instruction's `LotteryTicketDelta` input at tuple index 2.
pub rescue_encrypted_lottery_ticket_delta: RescueCiphertext,
/// Encrypted random blinding factor (single 256-bit Curve25519
/// field element, rescue-encrypted into one 32-byte ciphertext).
pub rescue_encrypted_random_factor: RescueCiphertext,
/// Polynomial evaluation for Fiat-Shamir encryption validation
pub encryption_validation_polynomial: FieldElement25519,
/// Poseidon hash commitment for Fiat-Shamir challenge
pub rescue_encryption_fiat_shamir_commitment: PoseidonHash,
/// H2 commitment for the unspent transaction output insertion
pub insertion_h2_commitment: PoseidonHash,
/// Unix epoch timestamp for the unspent transaction output creation
pub insertion_timestamp: UnixEpochTimestamp,
/// First Poseidon ciphertext for linker encryption
pub linker_encryption0: PoseidonCiphertext,
/// Second Poseidon ciphertext for linker encryption
pub linker_encryption1: PoseidonCiphertext,
/// Third Poseidon ciphertext for linker encryption
pub linker_encryption2: PoseidonCiphertext,
/// First keystream commitment
pub keystream_commitment0: PoseidonHash,
/// Second keystream commitment
pub keystream_commitment1: PoseidonHash,
/// Third keystream commitment
pub keystream_commitment2: PoseidonHash,
/// Groth16 proof element A (G1 point, 64 bytes)
pub groth16_proof_a: Groth16ProofA,
/// Groth16 proof element B (G2 point, 128 bytes)
pub groth16_proof_b: Groth16ProofB,
/// Groth16 proof element C (G1 point, 64 bytes)
pub groth16_proof_c: Groth16ProofC,
/// AES encrypted unspent transaction output data for off-chain storage/indexing
pub aes_encrypted_data: AesEncryptedUnspentTransactionOutputData,
/// Optional data field for application-specific metadata
pub optional_data: OptionalData,
pub reserved_byte: [ReservedByte; 0],
}


pub const STEALTH_POOL_DEPOSIT_INPUT_BUFFER_DISCRIMINATOR: [u8; 8] = [74, 79, 67, 234, 64, 203, 101, 220];

impl StealthPoolDepositInputBuffer {
      pub const LEN: usize = 899;
  
  
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for StealthPoolDepositInputBuffer {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_stealth_pool_deposit_input_buffer(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<StealthPoolDepositInputBuffer>, std::io::Error> {
  let accounts = fetch_all_stealth_pool_deposit_input_buffer(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_stealth_pool_deposit_input_buffer(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<StealthPoolDepositInputBuffer>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<StealthPoolDepositInputBuffer>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::new(std::io::ErrorKind::Other, format!("Account not found: {}", address)))?;
      let data = StealthPoolDepositInputBuffer::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_stealth_pool_deposit_input_buffer(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<StealthPoolDepositInputBuffer>, std::io::Error> {
    let accounts = fetch_all_maybe_stealth_pool_deposit_input_buffer(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_stealth_pool_deposit_input_buffer(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<StealthPoolDepositInputBuffer>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<StealthPoolDepositInputBuffer>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = StealthPoolDepositInputBuffer::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for StealthPoolDepositInputBuffer {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for StealthPoolDepositInputBuffer {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for StealthPoolDepositInputBuffer {
      fn owner() -> Pubkey {
        crate::UMBRA_ID
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for StealthPoolDepositInputBuffer {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for StealthPoolDepositInputBuffer {
    const DISCRIMINATOR: &[u8] = &[0; 8];
  }

