//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::VersionByte;
use crate::types::CanonicalBump;
use crate::types::SlotNumber;
use crate::types::Amount;
use crate::types::UnixEpochTimestamp;
use crate::types::AccountOffset;
use solana_pubkey::Pubkey;
use crate::types::PoseidonHash;
use crate::types::AesEncryptedUnspentTransactionOutputData;
use crate::types::OptionalData;
use crate::types::ArciumX25519PublicKey;
use crate::types::ArciumX25519Nonce;
use crate::types::DispatchObserverCpi;
use crate::types::InstructionDiscriminator;
use crate::types::ReservedByte;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct DepositIntoStealthPoolFromSharedBalanceV13ComputationData {
pub discriminator: [u8; 8],
pub version_byte: VersionByte,
pub canonical_bump: CanonicalBump,
/// Slot at which this MPC computation was queued. Set by the
/// queue handler from `Clock::get()?.slot`. Read by
/// `close_computation_data` for the cleanup TTL gate. Field
/// position (FIRST user field) is canonical across all
/// ComputationData structs so cleanup can read at a fixed offset.
pub queued_at_slot: SlotNumber,
/// The staged SOL amount (result of calculate_fixed_fees from queue computation)
/// Used directly as relayer_fixed_sol_fees in H1 hash calculation
pub staged_sol_amount: Amount,
/// Unix epoch timestamp for the unspent transaction output creation
/// Used for H1 hash calculation (split into year/month/day/hour/minute/second)
pub insertion_timestamp: UnixEpochTimestamp,
/// Index of the currently active incremental Merkle tree
/// From protocol_config.index_of_currently_active_incremental_merkle_tree
pub tree_index: AccountOffset,
/// Mint address for the token being deposited
/// Used in H1 hash computation
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub mint_address: Pubkey,
/// H2 commitment for the unspent transaction output insertion (provided by client)
pub insertion_h2_commitment: PoseidonHash,
/// AES encrypted unspent transaction output data for off-chain storage/indexing (96 bytes)
pub aes_encrypted_data: AesEncryptedUnspentTransactionOutputData,
/// Optional data field for application-specific metadata (32 bytes)
pub optional_data: OptionalData,
/// Depositor's X25519 public key for event emission in callback
pub depositor_x25519_public_key: ArciumX25519PublicKey,
/// X25519 pubkey the creator's output balance is encrypted to.
pub creator_balance_output_x25519_public_key: ArciumX25519PublicKey,
/// Output nonce supplied to Arcium for the creator balance output ctxt.
pub creator_balance_output_nonce: ArciumX25519Nonce,
pub fees_output_nonce: ArciumX25519Nonce,
pub observer_output_x25519_public_key: ArciumX25519PublicKey,
pub observer_output_nonce: ArciumX25519Nonce,
/// When DISABLED the callback skips the observer CPI (and the queue
/// handler encrypts the observer ciphertext to the MXE pubkey for
/// confidentiality). When ENABLED the destination_program /
/// cpi_account_1 / cpi_account_2 / destination_discriminator /
/// initiator fields below are honoured.
pub dispatch_observer_cpi: DispatchObserverCpi,
/// Destination program the callback CPIs into when observer is enabled.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub destination_program: Pubkey,
/// Anchor-style 8-byte discriminator of the destination's receive instruction.
pub destination_discriminator: InstructionDiscriminator,
/// First CPI account forwarded to the destination program.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub cpi_account1: Pubkey,
/// Second CPI account forwarded to the destination program.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub cpi_account2: Pubkey,
/// Independent initiator identity persisted at queue time; forwarded to
/// the destination program so it can verify the request it initiated.
#[cfg_attr(feature = "serde", serde(with = "serde_with::As::<serde_with::DisplayFromStr>"))]
pub initiator: Pubkey,
pub reserved_byte: [ReservedByte; 0],
}


pub const DEPOSIT_INTO_STEALTH_POOL_FROM_SHARED_BALANCE_V13_COMPUTATION_DATA_DISCRIMINATOR: [u8; 8] = [89, 48, 234, 226, 221, 118, 156, 105];

impl DepositIntoStealthPoolFromSharedBalanceV13ComputationData {
      pub const LEN: usize = 524;
  
  
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for DepositIntoStealthPoolFromSharedBalanceV13ComputationData {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_deposit_into_stealth_pool_from_shared_balance_v13_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<DepositIntoStealthPoolFromSharedBalanceV13ComputationData>, std::io::Error> {
  let accounts = fetch_all_deposit_into_stealth_pool_from_shared_balance_v13_computation_data(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_deposit_into_stealth_pool_from_shared_balance_v13_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<DepositIntoStealthPoolFromSharedBalanceV13ComputationData>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<DepositIntoStealthPoolFromSharedBalanceV13ComputationData>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::new(std::io::ErrorKind::Other, format!("Account not found: {}", address)))?;
      let data = DepositIntoStealthPoolFromSharedBalanceV13ComputationData::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_deposit_into_stealth_pool_from_shared_balance_v13_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<DepositIntoStealthPoolFromSharedBalanceV13ComputationData>, std::io::Error> {
    let accounts = fetch_all_maybe_deposit_into_stealth_pool_from_shared_balance_v13_computation_data(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_deposit_into_stealth_pool_from_shared_balance_v13_computation_data(
  rpc: &solana_client::rpc_client::RpcClient,
  addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<DepositIntoStealthPoolFromSharedBalanceV13ComputationData>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<DepositIntoStealthPoolFromSharedBalanceV13ComputationData>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = DepositIntoStealthPoolFromSharedBalanceV13ComputationData::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for DepositIntoStealthPoolFromSharedBalanceV13ComputationData {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for DepositIntoStealthPoolFromSharedBalanceV13ComputationData {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for DepositIntoStealthPoolFromSharedBalanceV13ComputationData {
      fn owner() -> Pubkey {
        crate::UMBRA_ID
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for DepositIntoStealthPoolFromSharedBalanceV13ComputationData {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for DepositIntoStealthPoolFromSharedBalanceV13ComputationData {
    const DISCRIMINATOR: &[u8] = &[0; 8];
  }

